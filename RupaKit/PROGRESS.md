# Progress

- [x] T09-0 Agent-ready Authored Mesh editing design hierarchy and executable contract are the sole non-conflicting design authority, reviewed and committed `depends:none` `parallel:none`
  - [x] T09-0.1 Create the reachable system, package, and changed-module `DESIGN.md` hierarchy; link the existing CAD/Mesh and project-state normative contracts without duplicating their decisions, and record parent/child/dependency relationships plus behavioral verification ownership `depends:none` `parallel:none`
  - [x] T09-0.2 Fix the exact T09 contract and non-goals: one source/plan/buffer/commit/undo, source-ID plus content-identity authority, ordered unique step IDs, prior-step-only output selectors, explicit finite global offsets, connected orientable manifold face-region extrusion, persistent ID allocation, RupaGeometry-owned hard ceilings with caller-lowerable effective limits, typed failures, exact-snapshot read/preview/commit, zero-copy evidence, and no AgentProtocol/CLI/MCP changes `depends:T09-0.1` `parallel:none`
  - [x] T09-0.3 Review every changed or superseded normative statement for single-operation, scene/representation-owned Mesh edits and authority ambiguity; remove conflicting legacy claims, validate all design links, and commit as `Define agent-ready Mesh editing contracts` `depends:T09-0.2` `parallel:none`
- [x] T09-A RupaGeometry executes one bounded declarative Mesh plan against one mutable staging owner and publishes one immutable result, reviewed and committed `depends:T09-0` `parallel:none`
  - [x] T09-A.1 Add the provider-independent plan contract: `GeometryVector3D`, stable step IDs, explicit and prior-output selectors, primitive edits, semantic translate/extrude operations, step outputs, execution receipt, executor protocol, validated limits, and typed errors; reject empty plans, duplicate IDs, forward/missing output references, invalid domains, non-finite values, overflow, and caller limits above the executor ceiling `depends:T09-0` `parallel:t09a-foundation`
  - [x] T09-A.2 Extend `MeshEditBuffer` to stage new vertices and exact face topology with vertex/edge/face/corner IDs allocated before commit, resolve both retained and staged elements, preserve deleted-ID non-reuse, and commit once while sharing untouched chunks and attributing every required copy `depends:T09-0` `parallel:t09a-foundation`
  - [x] T09-A.3 Implement the default executor over exactly one `MeshEditBuffer`: run steps in order, expose immutable outputs to later selectors, expand translate selections to one deduplicated vertex set, and extrude one connected consistently-oriented manifold face region by duplicating each selected vertex once, replacing selected faces with caps, and adding boundary side faces; reject topology edits on attributed Meshes until explicit attribute remapping exists `depends:T09-A.1,T09-A.2` `parallel:none`
  - [x] T09-A.4 Prove primitive parity, multi-step output chaining, deterministic persistent IDs, repeated-face-region extrusion, mixed-domain rejection, disconnected/inconsistently-oriented/non-manifold rejection, no-op behavior, budget boundaries, mid-plan rollback, attribute-preserving translation, typed topology-attribute failure, unchanged chunk identity, and measured copy ceilings with focused RupaGeometry tests; audit incomplete/conditional/unsafe/shared-state markers, review the full Geometry path, and commit as `Execute bounded Authored Mesh edit plans` `depends:T09-A.3` `parallel:none`
- [x] T09-B RupaCore applies a Mesh plan to the Authored Mesh asset authority without changing Product selection, CAD, provenance, or references, reviewed and committed `depends:T09-A` `parallel:none`
  - [x] T09-B.1 Replace `AuthoredMeshEditTarget` and one-operation `AuthoredMeshEditCommand` with source-authority target and plan command types; migrate `GeometrySourceCommand` and results to carry the plan receipt and copy telemetry, remove the old public API and decoder surface, and update every production/test call site without compatibility fallback `depends:T09-A` `parallel:none`
  - [x] T09-B.2 Inject the plan executor into `DefaultGeometrySourceCommandApplier`, validate source/content identity once before execution, replace only the matching asset after full plan success, retain provenance and all Product/CAD/selection bytes, and make a shared asset edit visible through every existing representation reference without implicit duplication or synchronization `depends:T09-B.1` `parallel:none`
  - [x] T09-B.3 Prove current/stale/missing source behavior, no-op identity stability, plan failure rollback, shared-reference propagation, retained-but-unselected asset editing, one command-history entry, undo/redo, and byte-for-byte CAD/Product-selection/provenance invariance with focused RupaCore tests; review the authority path and commit as `Apply Mesh plans through source authority` `depends:T09-B.2` `parallel:none`
- [ ] T09-C RupaKit exposes transport-neutral bounded Mesh read and preview/commit use cases over the existing ProjectController authority, reviewed and committed `depends:T09-B` `parallel:none`
  - [ ] T09-C.1 Add immutable project Mesh source handles bound to project authority coordinate, source ID, and content identity plus `ProjectMeshReading` catalog, paged element, and bounded neighborhood contracts; catalog provenance, element counts, bounds, all referencing Objects/representations, and purpose selections, bind cursors to exact source content, materialize only bounded response records, execute heavy scans outside the project actor, and revalidate authority and cancellation before returning `depends:T09-B` `parallel:none`
  - [ ] T09-C.2 Add `ProjectMeshEditing` preview and commit requests/results on `ProjectWorkspace`; require request handle and supplied `ProjectViewSnapshot` coordinates to agree, lower the plan into one existing `ProjectSourceTransaction`, reuse existing preview/atomic commit/view publication paths, return exact plan outputs and new source handle, and never promote preview output into source authority `depends:T09-C.1` `parallel:none`
  - [ ] T09-C.3 Prove catalog reference accuracy, stable pagination, cursor/content mismatch, neighborhood depth/scan/output limits, cancellation and stale pre/post-read coordinates, preview non-publication, commit exact-view publication, stale handle rejection, one revision/undo, post-commit view failure no-retry behavior, and Mesh-only/mixed routing with focused RupaKit and RupaProject tests; review the end-to-end use-case path and commit as `Expose project Mesh read and edit use cases` `depends:T09-C.2` `parallel:none`
- [ ] T09-IV T09 integration verification proves inspect-to-save behavior and all cumulative authority, atomicity, persistence, and zero-copy invariants, reviewed and committed `depends:T09-0,T09-A,T09-B,T09-C` `parallel:none`
  - [ ] T09-IV.1 Exercise real catalog-to-handle-to-page/neighborhood-to-preview-to-commit-to-presentation-evaluation-to-undo/redo-to-save/load workflows for Mesh-only and CAD-plus-Authored-Mesh packages, including multi-step extrude/translate output chaining and shared-source references `depends:T09-C` `parallel:none`
  - [ ] T09-IV.2 Inject plan, source validation, cancellation, stale coordinate, package preparation, reconstruction, evaluation, and prepublication save-preparation failures and prove that every published state remains atomically unchanged; separately inject save failure after an already-published edit and prove that the committed edit and publication remain intact, the session remains dirty, and saving is retryable `depends:T09-IV.1` `parallel:none`
  - [ ] T09-IV.3 Prove source-to-evaluation unchanged-buffer identity, bounded response materialization, one-buffer copy telemetry, required topology-copy attribution, deterministic save/load output, unchanged blob reuse, and superseded blob garbage collection on the actual ProjectController path `depends:T09-IV.2` `parallel:none`
  - [ ] T09-IV.4 Run timeout-bounded focused and affected Native build/tests, compile/link the changed portable targets with the pinned ordinary and Embedded WASM toolchain where their dependency graph supports it, report non-runnable target semantics without treating compile as runtime proof, repeat incomplete/conditional/synchronization/unsafe/mutable-state audits, perform one cumulative production review with zero scope findings, synchronize design/progress, and commit as `Verify Agent-ready Mesh editing` `depends:T09-IV.3` `parallel:none`
