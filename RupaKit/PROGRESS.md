# Progress

- [x] T12-0 The Agent CAD benchmark responsibility, design hierarchy, measurement boundary, exactly-100-case target envelope, non-goals, invariants, and falsifiable evidence contract are approved, reviewed, and committed as `Define Agent CAD benchmark contract` `depends:none` `parallel:none`
- [x] T12-F The committed minimum vertical-case foundation required by LIN-001 is preserved and its candidate-result contract matches the confirmed production primary/created FeatureID alias semantics; focused regression tests and designer review are complete, and the correction is committed as `Align benchmark feature references with production results` `depends:T12-0` `parallel:none`
- [x] T12-LIN All twelve lexical line cases completed individual production-controller, exact-source-oracle, adversarial, timeout, cleanup, telemetry, privacy, focused, and original-designer gates at serial concurrency one; case commits `70a67d89`, `700ff44e`, `60e458ec`, `2183db93`, `c9e8ac7a`, `41091a65`, `12cb9176`, `70a9e7c8`, `8035a7fd`, `bcd6c8cd`, `635c2f00`, and `dab3c176`, the first-ten checkpoint `597b849c`, and category gate `2f69f5f0` preserve the complete line evidence `depends:T12-F` `parallel:none`
- [x] T12-REC The explicit-center foundation `966a8176`, twelve serial production-route rectangle case commits `7c31df61`, `b7cb32f2`, `6d7adeec`, `d34f955d`, `0cb15932`, `4c4bc454`, `5f23c426`, `dd5e88b7`, `b0707d61`, `647b54ff`, `4adb9171`, and `7e3d3876`, first-twenty checkpoint `27a31a0c`, external-Agent design/executor/ID/adapter/CLI/review commits `8d376343`, `f9f77e3e`, `738af53d`, `19247c9b`, `bf17eeb8`, and `dc5e9a99`, and rectangle gate `330ad616` preserve exact plane/unit/source-oracle, failure, cleanup, telemetry, privacy, bounded wire/process, and authority evidence `depends:T12-LIN` `parallel:none`
- [x] T12-CIR All twelve circle cases completed serial production-controller, exact analytic source/profile oracle, adversarial/no-retry, prepublication, timeout, cleanup, telemetry, privacy, JSON/CLI, and original-designer gates in commits `1a86ab95`, `31707189`, `6b02e47b`, `89c0095e`, `c3018022`, `8e1771b5`, `878a9065`, `c5563369`, `2990577f`, `2740090d`, `4054fb37`, and `491a9d11`, followed by category gate `a6f2f125` `depends:T12-REC` `parallel:none`
- [x] T12-ANG All sixteen ordered angle/intersection cases completed serial production atomic-batch, exact intersection/angle/source oracle, independent postpublication no-retry, prepublication, timeout, cleanup, telemetry, privacy, authority, JSON/CLI, and original-designer gates in commits `3adef2d1`, `357b2878`, `3ae28a53`, `3f524e77`, `435bd003`, `692a82d8`, `6b03dcdc`, `62d7cfd7`, `4e423e88`, `8f26b1a4`, `36f0870b`, `ced1fc07`, `c199ee29`, `c3200a98`, `72118faa`, and `81ba8198`, followed by category gate `9021870f` `depends:T12-CIR` `parallel:none`
- [x] T12-BOX All twelve box cases completed serial production extruded-rectangle, exact authored-source/B-Rep/topology oracle, adversarial/no-retry, rollback, timeout, cleanup, telemetry, privacy, authority, JSON/CLI, and original-designer gates in commits `95c9eaeb`, `046dcbf6`, `833e9703`, `ed2d190e`, `600a81bb`, `78be0d81`, `7c7da25c`, `695d1db9`, `e2c40129`, `fa45e5af`, `81f1969b`, and `20a9382b`, followed by category gate `dd455412` `depends:T12-ANG` `parallel:none`
- [x] T12-CYL All eight cylinder cases completed serial production extruded-circle, exact oriented authored-source/B-Rep/topology oracle, adversarial/no-retry, invalid-axis/radius/depth, timeout, cleanup, telemetry, privacy, authority, JSON/CLI, and original-designer gates in commits `7e6ae155`, `93f6455e`, `d42f8454`, `5a346cce`, `e1ca223b`, `140eb704`, `ad838202`, and `f50657e5`, followed by category gate `20bd5656` `depends:T12-BOX` `parallel:none`
- [x] T12-CON All eight constraint cases completed serial production sketch transactions, exact authored relation/geometry and derived-region oracles, postpublication no-retry, prepublication invalid-shape/geometry, timeout, cleanup, telemetry, privacy, authority, JSON/CLI, and original-designer gates in commits `a105dd93`, `66007b07`, `dc1c2fb5`, `a6aedb90`, `6c315a45`, `3cf564c9`, `3c8994a2`, and `3881e1a9`, followed by category gate `83480e59` `depends:T12-CYL` `parallel:none`
- [x] T12-R20-F Before parallel preparation, the shared remaining-target contract adds an explicit public/private `axisPoint` to all eight transform catalog inputs using the source geometric centre in source-world coordinates—TRN-001 (50,0,0), TRN-002 (0,0,0), TRN-003 (0,0,0), TRN-004 (10,15,20), TRN-005 (0,0,20), TRN-006 (0,0,0), TRN-007 (0,0,0), and TRN-008 (25,-25,0), all millimetres—and fixes column-vector placement as `world = Translation(translation) * Translation(axisPoint) * Rotation(normalizedAxis, angle) * Translation(-axisPoint) * source`; canonical instructions expose the complete transform-source orientation by including each line/rectangle/circle sketch plane and each cylinder axis, and also make every compound cylinder member's existing private axis public as `along axis (x,y,z)`, preserving member order and role so all TRN-001...008 targets and CMP-001/003/004/006/007 are reconstructible from candidate-visible data; no implicit pivot, axis, centroid, primitive orientation, or composition fallback is permitted, non-finite points and zero axes fail typed, manifest catalog version advances `t12.catalog.v4`→`v5` and private expectation schema/contract advance `v3`→`v4` with observed literal challenge/manifest/expectation digests while manifest schema v2, tolerance, capability baseline, activated authority, candidate-response v6, and the frozen 80-request aggregate `91f68ea42c6e131263b499995637e9f9b7dbce64fcf441bdcdf385c9f341efb0` remain unchanged, all existing 80 production cases regress, original-designer review and diff/static audits pass, and commit `Define remaining CAD target semantics` completes without activating TRN/CMP/SPH or changing executor/JSON/CLI authority `depends:T12-CON` `parallel:none`
- [x] T12-R20-P The authority-neutral lifecycle foundation commits `b0c79f54` and `5aa231bc` preserve all 80 activated cases while adding seeded-source and common preflight-cancellation contracts; the original T12 designer approves the preparation commits `e1583db1` (TRN), `98e4c4dc` (CMP), and `42ef6f57` (SPH) against their category contracts after focused behavioral verification, confirms all three have exact parent `5aa231bc`, their 38 owned `CADTransform*`/`CADCompound*`/`CADSphere*` production-and-test paths are pairwise disjoint with no shared authority/schema/digest edits, and fixes the integration order as TRN then CMP then SPH `depends:T12-R20-F` `parallel:none`
- [x] T12-TRN The reviewed transform preparation commit `e1583db1` and eight serial case commits `a56a532b`, `f3cf4732`, `84f2b177`, `c72fad57`, `2ccb78ca`, `cc2a4207`, `04dbc804`, and `ae245a8b` activate TRN-001...008 through the production candidate/controller/oracle route with exact source/B-Rep/placement, failure, telemetry, cleanup, focused benchmark/adapter/actual-CLI, static, and original-designer evidence; external authority is exactly 88 with candidate-response v7 and measured aggregate digest `8a8c89deee4c596749cd6411823ab475bfd13ef7da7ccbff1c66b0425ee53795`, CMP-001 remains typed inactive, and the serialized cumulative checkpoint plus final no-finding review complete in commit `Verify Agent transform benchmark category` without changing production behavior or shared contracts `depends:T12-R20-P` `parallel:none`
- [x] T12-CMP The reviewed compound preparation commit `aec8434c` and seven serial case commits `d94247e2`, `47f1e435`, `ae5d2154`, `b6fc0bff`, `4101b048`, `74d74aa0`, and `60f4f962` activate CMP-001...007 in exact catalog order through one public ordered role-bearing compound action, stable member-derived capabilities, all-member pre-lowering, one production candidate/controller atomic batch, and the immutable role-sensitive authored-source/B-Rep oracle; the committed success, postpublication no-retry, prepublication invalid-member, two/three-command rollback, candidate failure, timeout/stale/cancellation, telemetry, and cleanup evidence composes across two- and three-member box/cylinder arrangements, including CMP-007's independent positive `bore` body rather than boolean subtraction; executor/JSON adapter/actual CLI authority is exactly 95 with candidate-response v8, measured aggregate digest `f9060999bba0bfa121bca51593fe5468c67b0237eebb0b0f82acd2feaf66fe1a`, and SPH-001 typed inactive; the serialized cumulative checkpoint passes 5 tests in 1 suite, prior same-snapshot full internal 586, JSON 62, and actual CLI 31 evidence remains valid, diff/static audits and original-designer cumulative review have zero P0/P1/P2 findings, and commit `Verify Agent compound benchmark category` completes without production, public API, schema, catalog, manifest, expectation, tolerance, lifecycle, kernel, Mesh, renderer, or sphere activation changes `depends:T12-TRN` `parallel:none`
- [x] T12-SPH The reviewed sphere preparation commit `542918b6` and five serial case commits `cc356769`, `f3ce7a68`, `ac84c3a7`, `8e96b98e`, and `3f43471f` activate SPH-001...005 only after the compound gate through the production controller capability observation and shared request/live classifier; absent Agent sphere ingress remains honestly typed `analyticSphereUnavailable` -> `expectedUnsupported`, all cases preserve one capability request/read with zero action/command/source mutation/geometry/publication, exact coordinate stability and zero-registration cleanup, while committed substitute, wrong analytic center, nonanalytic surface, invalid unsupported, candidate failure, timeout, and cancellation evidence composes without fabricating sphere geometry; activated and prepared sphere IDs are exactly equal, executor authority is the complete 100-case catalog by count and set equality with category order distinct from catalog lexical order, candidate-response v8 and the frozen 99-request aggregate remain unchanged, and the measured 100-request aggregate is `e7dac9bbeeec1b6f5198d9a89c4a8254eabba74df39389576fc2fdc4956b2767`; the serialized cumulative checkpoint passes 1 test, full internal 611 and unchanged same-snapshot JSON 67, actual CLI 36, and authority-mirror 12 evidence remains green, diff/static/privacy audits and original-designer cumulative review have zero P0/P1/P2 findings, and commit `Verify Agent sphere benchmark category` completes without production, public API, schema, catalog, manifest, expectation, tolerance, kernel, Mesh, renderer, or aggregate-runner changes `depends:T12-CMP` `parallel:none`
- [x] T12-I All 100 reviewed case paths compose into complete serial replay, bounded concurrency measurement with conservative policy one and a 38-second deadline, immutable capability/execution baselines, fixed 95/5 and capability 100/100 scoring, deterministic 9,992-byte report under the 16,384-byte ceiling, cancellation/drain/cleanup, static/privacy, and original-designer evidence in commits `34594d4c`, `80cb9a84`, `6dd495c5`, and `32d564f5` `depends:T12-LIN,T12-REC,T12-CIR,T12-ANG,T12-BOX,T12-CYL,T12-CON,T12-TRN,T12-CMP,T12-SPH` `parallel:none`
- [x] T12-IV The original T12 task designer reviewed every cumulative artifact and actual execution path against all T12 invariants with no remaining P0/P1/P2 finding; all 100 stable target specifications have individual production-route, exact-oracle, failure, telemetry, focused-test, gate-review, and commit evidence; shared foundations, fixed TRN→CMP→SPH integration, serial-before-measurement publication, bounded external JSON/CLI authority, candidate/oracle separation, rollback, geometry semantics, resource bounds, source/B-Rep authority, honest sphere capability, immutable baseline drift, deterministic reporting, cleanup, and no-retry contracts hold; 625 internal non-performance, 1 isolated integrated-policy, 67 JSON-adapter, and 36 actual-CLI tests pass; static/privacy audits and diff-check pass; commit history, branch, PROGRESS, and the complete DESIGN hierarchy are synchronized for the final evidence commit and normal same-branch upstream publication `depends:T12-0,T12-F,T12-LIN,T12-REC,T12-CIR,T12-ANG,T12-BOX,T12-CYL,T12-CON,T12-R20-F,T12-R20-P,T12-TRN,T12-CMP,T12-SPH,T12-I` `parallel:none`
- [x] V8-0 The professional V8 product target, claim boundary, primary-source provenance, design hierarchy, subsystem responsibilities, unresolved decisions, and falsifiable acceptance contract are fixed and committed as `Define professional V8 engineering contract` `depends:T12-IV` `parallel:none`
- [x] V8-T The deterministic 450 kW curve and independent tests close power, BMEP, fuel/air, compressor coordinates, first-law allocation, turbine-temperature screening, and HT/oil/LT flow/capacity gates; they reject the original over-temperature heat split and are committed as `Calculate professional V8 thermal budgets` while exact compressor-map selection, 1D gas exchange, vehicle cooling, and dyno evidence remain explicit P3/P4 gates `depends:V8-0` `parallel:none`
- [x] V8-M The hash-bound mechanical model closes deck and bank geometry, screens 20 MPa gas and 7,000/7,350 rpm inertia loads, rod Euler capacity, pin/crankpin projected pressure, head clamp, valve area, fuel-per-cycle, thermal interfaces, and generates 168 unique semantic part identities plus 19 datums; deterministic success/failure evidence is committed as `Define professional V8 mechanical architecture` without treating P2 screens as FEA, oil-film, fatigue, sealing, torsional, or durability approval `depends:V8-T` `parallel:none`
- [x] V8-C The public Rupa Agent CLI publishes 202 manifest-bound source bodies covering all 168 semantic parts in one 203-command history entry and evaluation pass; the native CAD independently reloads with exact 86 mm bore and 65 mm main-journal profiles, 202-body B-Rep and Viewer Mesh evaluation, explicit envelope-only unsupported-detail gates, and is committed as `Generate professional V8 semantic CAD` `depends:V8-M` `parallel:none`
- [x] V8-V The cumulative hash-bound package passes 19 deterministic calculation, failure, stale-evidence, semantic-coverage, atomic-publication, source/B-Rep, save/load, viewer-Mesh, claim-boundary, and commit-history checks; `integration-report.json` accepts only the P2 professional engineering reference while preserving 19 explicit production-release blockers, and the final evidence is committed as `Verify professional V8 engineering reference` `depends:V8-0,V8-T,V8-M,V8-C` `parallel:none`
- [x] RUPA-VIEW-0 State-driven viewport scheduling and bounded source-order Mesh triangulation pass focused Geometry 81/81 and Rendering 355/355 tests, actual signed Rupa App empty-idle CPU averaging 0.01 percent, actual live-CLI cylinder publication and visible viewport proof, final designer review, and commit `Fix idle viewport and bound mesh triangulation` `depends:V8-V` `parallel:none`
- [x] ACCESS-A The transport-neutral project-access authority contract, semantic/transport separation, focused ProjectAccess and Transport behavior evidence, stale-boundary audits, and original-designer review are complete and committed as `Define ProjectController project-access authority` `depends:RUPA-VIEW-0` `parallel:none`
- [x] ACCESS-O The single App-owned `ProjectWorkspace`/`ProjectController` now owns schema-v3 `.rupa` open, process-lifetime Agent routing, sandbox-compatible validated atomic package save, explicit API save, typed stale and postpublication failure, exact current-URL lifetime, `.rupa`-only application metadata, and single-process authority; focused Package/App/Agent tests, default-signed App build, actual LaunchServices plus CLI mutation/stale-save/explicit-save/terminate/reload proof, nonempty viewport evidence, and original-designer review pass, and commit `Open and save schema-v3 projects through Rupa authority` completes the sprint `depends:ACCESS-A` `parallel:none`
- [x] BIKE-M The currently opened `/Users/1amageek/Desktop/3D/RupaVerification.rupa` is upgraded only through the actual signed-App Agent API and the same App-owned `ProjectWorkspace`/`ProjectController`: the two solid-disc wheel presentations are replaced by coherent tire/rim/hub/spoke assemblies, and a crankset with a visibly toothed chainring, opposed left/right crank arms, and two pedals is added without changing the existing frame, fork, saddle, handlebar, project identity, units, or unrelated source; no direct archive/source-JSON edit, closed-file fallback, private `EditorSession`, UI mutation, manufacturing/safety claim, chain, rear sprocket, derailleur, brake, or suspension work is in scope; immutable API readback, evaluation, topology/measurement evidence, and original-designer review are required before persistence `depends:ACCESS-O` `parallel:none`
  - [x] BIKE-M.1 Start from exactly one signed Rupa authority process, resolve `RupaVerification.rupa` by canonical path through actual `rupa sessions`/`attach`, capture its session/project/generation/revision/publication/workspace coordinates and pre-save file hash, and use immutable Agent reads to record the current wheel centers/envelopes, existing front/rear solid-disc scene nodes, crank center, and preserved frame/fork/saddle/handlebar identities; reject a path/session/authority mismatch before mutation and do not use package extraction as mutation or completion evidence `depends:ACCESS-O` `parallel:none`
  - [x] BIKE-M.V Define Core's hierarchy-aware effective scene-node visibility without deleting or altering retained Product/CAD/Mesh source, filter only effectively visible presentation occurrences when building `ProjectViewSnapshot.viewport` while preserving the complete occurrence-to-scene navigation index, and prove through an actual signed-App Agent source transaction that hiding a parent or body retains evaluation/source identity but removes its viewport item, world-bounds contribution, and rendered geometry, then restoring it reverses only visibility; focused Core/Project/App tests, actual App readback and viewport proof, original-designer review, and one coherent commit are required `depends:BIKE-M.1` `parallel:none`
  - [x] BIKE-M.T Bound RupaViewportScene's B-spline surface-overlay topology read by skipping it when no B-spline surface feature exists and otherwise sharing at most one identity-only `TopologySnapshotService(metricPolicy: .omit)` result across knot/span lookup, without requesting face-area or edge-length metrics or changing generated geometry and stable patch references; prove an actual torus/revolve `ViewportSceneBuilder` build completes within the existing Agent request deadline with zero topology metric evaluations, B-spline knot/span overlays remain exact, cancellation/resource limits remain bounded, and a default-signed App restored from the last saved BIKE-M.V project serves `sessions`/`attach`/immutable viewport reads without sustained CPU saturation; focused Core/ViewportScene/App tests, original-designer review, one coherent commit, and discarding rather than saving the unsaved generation-36 trial are required `depends:BIKE-M.V` `parallel:none`
  - [x] BIKE-M.2 Through one exact-session source transaction or an explicitly preflighted sequence whose every published coordinate is carried forward, hide the two existing solid-disc presentation nodes and add front/rear full-revolution tire bodies with rounded cross-sections and the same measured centers and outer envelopes, separate inner rims, centered hubs, and at least eight evenly distributed nonzero-section spokes per wheel; no tire body may contain a filled face spanning its hub opening, and the prior authored Mesh assets may remain stored only if they are no longer visible or presentation-selected `depends:BIKE-M.T` `parallel:none`
  - [x] BIKE-M.3 Through the same authority add one chainring centered on the bottom bracket with at least twelve evenly distributed individually measurable teeth, two nonzero-length crank arms opposed by 180 degrees on opposite sides of the bicycle midplane, and one distinct nonzero-area pedal platform at each crank end; names and source/body identities are unique, the chainring is not represented by a plain disc, and any command, evaluation, stale-coordinate, cancellation, or resource failure is reported without direct file repair or alternate authority `depends:BIKE-M.2` `parallel:none`
  - [x] BIKE-M.4 Use immutable Agent design-display, topology, mesh, and measurement reads at the final publication to prove the preserved parts are unchanged, both wheel openings/rims/hubs/spokes and all crankset members exist at coherent centers/planes with nonempty finite evaluated geometry, the hidden discs do not contribute presentation triangles, and the viewport snapshot is nonempty; perform one bounded source/behavior review and repair only concrete BIKE-M contract findings before persistence `depends:BIKE-M.3` `parallel:none`
- [x] CADAPI-D The one-vocabulary/exactly-two-form composable Agent CAD API, bounded source-only DAG, server-owned persistent identity, native-pattern compactness, single ProjectWorkspace-to-ProjectController publication, typed failure/no-retry, explicit-save separation, legacy-boundary honesty, hierarchy/source/manifest audits, and original-designer approval are fixed and committed as `Define composable Agent CAD API` (`d041b47f`) `depends:ACCESS-O,BIKE-M` `parallel:none`
- [ ] BIKE-IV The ACCESS-O and BIKE-M results compose end to end through the actual API: the exact open `RupaVerification.rupa` session is explicitly saved without UI, the App is terminated only after acknowledged clean publication and durable hash change, a fresh signed Rupa process reloads the same file, `sessions`/`attach` and immutable reads reproduce the saved project/content identity and bicycle geometry, and the actual Rupa window visibly shows separated rounded tires, rims/spokes, a toothed chainring, opposed cranks, and both pedals; destination/hash preservation on stale or failed save, one process/one workspace authority, no direct package edits, no unrelated dirty changes, original-designer conformance approval, artifact commit `Upgrade Rupa bicycle model through project API`, and the final evidence commit are required `depends:ACCESS-O,BIKE-M` `parallel:none`
  - [x] BIKE-IV.1 Send explicit `.save` through the actual `rupa` CLI with the exact session and expected generation, prove the router/coordinator/workspace/controller/package telemetry path, dirty-to-clean transition, changed durable destination hash, and unchanged bytes for a separate stale-generation or injected prepublication failure; do not use Computer Use, menu commands, Save panels, direct ZIP/JSON writes, or a closed-file mutation fallback `depends:ACCESS-O,BIKE-M` `parallel:none`
  - [x] BIKE-IV.2 After acknowledged save, terminate the sole Rupa process, reopen the same canonical file through LaunchServices, re-resolve one session by path, and prove through API coordinates, source/content identity, design-display/topology/mesh/measurement reads, and stable on-disk hash that the fresh `ProjectWorkspace.load`/`ProjectController` publication contains every BIKE-M invariant and no visible solid wheel disc `depends:BIKE-IV.1` `parallel:none`
  - [ ] BIKE-IV.3 Capture and inspect an uncomposited screenshot of the actual reloaded Rupa window without using UI to mutate or save, require the two wheel openings and rounded tires, rims/spokes, toothed chainring, opposed cranks, and both pedals to be visually distinguishable, reconcile it with the same API publication rather than treating the image alone as geometry proof, audit the executed route and Git diff for direct-file/fallback/unrelated changes, obtain original-designer approval, fold the completed BIKE items with their commit, and commit the final integrated evidence `depends:BIKE-IV.2` `parallel:none`
- [x] ACCESS-C Live project access launches or attaches to the single-authority Rupa application, resolves its registered App-owned workspace, carries semantic requests through one injected bounded transport adapter, delegates explicit save to the same lifecycle coordinator and `ProjectController`, preserves process-lifetime host, deadline, cancellation, no-fallback, and outcome-unknown/no-retry contracts, and is committed as `Integrate live Rupa project access` (`3afcb7eb`) `depends:ACCESS-O` `parallel:none`
- [x] ACCESS-D Production `rupa` project commands delegate semantic intent and explicit save through one injected async `RupaProjectAccess` composition without direct package/session/Automation mutation, production endpoint selection, force-file-edit, `.swcad` mutation, or silent authority fallback, and are committed as `Route CLI through project access` (`2659ce12`) and `Distribute signed Rupa CLI` (`b8c93cec`) `depends:ACCESS-C` `parallel:none`
- [x] ACCESS-IV RUPA-ACCESS exposes only the transport-neutral `RupaProjectAccess` API to the signed distribution CLI and future explicitly credentialed adapters and carries that API over a mutually authenticated loopback HTTP adapter into the single App-owned `ProjectWorkspace`/`ProjectController`; App Group, UDS, filesystem endpoint discovery, bearer-secret transmission, same-UID inference, unsigned production clients, alternate local-file mutation authority, temporary external controllers, and compatibility paths are absent, while explicit save, one monotonic deadline, rollback, bounded resources, no fallback, and post-dispatch no-retry remain mandatory; completion requires focused and actual signed App/CLI evidence with zero P0/P1/P2 findings before one corrective commit `depends:ACCESS-A,ACCESS-O,ACCESS-C,ACCESS-D` `parallel:none`
  - [x] ACCESS-IV.0 Revise the affected system, package, module, App, CLI, transport, access-composition, and platform designs before source changes: bind dynamic loopback TCP; publish only port, launch generation, and a per-launch 256-bit HMAC-SHA256 key in a Team-owned Keychain access-group record; require one authenticated exchange per TCP connection—exactly `POST /v1/challenge` with a fresh client nonce, a server nonce and generation/port-bound server proof before any semantic body is sent, then at most one `POST /v1/rpc` on that same still-open connection with a body-digest-bound client proof, single-use challenge, request ID, and authenticated response proof; never reconnect or retry between proof verification and RPC, classify disconnect before semantic-body transmission as `notDispatched` and any disconnect after the complete body is sent as `outcomeUnknown`, and reject stale generation, expired/replayed challenge, invalid proof, and mismatched response before decode/publication/success; make only the project-default signed Xcode `rupa` product a production discovery client while test/future adapters must inject an explicit endpoint and key, and remove the unsigned SwiftPM executable product rather than claiming equivalent production behavior; retain App-sandbox authority and process-lifetime host ownership plus 16-MiB body, 32-connection, bounded-header, required-content-length, deadline, cancellation, shutdown, and no-retry contracts; remove every App Group, UDS, temporary-root, same-UID, closed-file, bearer, and compatibility claim from current designs `depends:ACCESS-A,ACCESS-O,ACCESS-C,ACCESS-D` `parallel:none`
  - [x] ACCESS-IV.1 Replace the complete App-Group/UDS/live-plus-closed implementation with the mutually authenticated loopback HTTP live API and signed product composition, without compatibility paths, while preserving the one App-owned workspace/controller, explicit-save, bounded-resource, deadline, cancellation, and no-retry contracts; the two disjoint lower-layer changes may run in parallel and App/product integration begins only after both are complete `depends:ACCESS-IV.0` `parallel:none`
    - [x] ACCESS-IV.1A In `RupaAgentTransport` and `RupaProjectAccessPlatform` only, replace Unix socket/address/same-UID code and App Group endpoint resolution with bounded loopback HTTP framing, same-connection HMAC challenge/RPC/response authentication, single-use challenge state, typed dispatch disposition, and Keychain discovery/key provider contracts plus focused transport/platform tests; do not edit Package.swift, access/CLI targets, App/Xcode composition, entitlements, lifecycle, or project authority `depends:ACCESS-IV.0` `parallel:access-iv-source`
    - [x] ACCESS-IV.1B In `RupaProjectAccess`, `RupaProjectAccessComposition`, `RupaCLIKit`, and Package.swift only, reduce targets to live project/session access, remove the legacy local-project target and access composition, cross-process project-file lease/crash probe, CLI local-file mutation/output routes, and the SwiftPM `rupa` executable entry/product, update the affected contract/composition/CLI tests, and preserve transport injection without implementing or selecting its concrete HTTP/Keychain composition; do not edit transport/platform internals, App/Xcode composition, entitlements, or lifecycle `depends:ACCESS-IV.0` `parallel:access-iv-source`
    - [x] ACCESS-IV.1C Integrate ACCESS-IV.1A and ACCESS-IV.1B in `RupaCLIComposition`, `RupaAgentUI.AgentHost`, Rupa App, native CLI target, Xcode project, and entitlements: make the App the only discovery-record writer, generate a new key/generation per listener lifetime, publish after readiness, remove only the matching generation after bounded drain, replace stale records only after application-authority acquisition, start the host independently of window/scene restoration, keep application and current-project identity fences inside the App sandbox container, add only App network-server and shared Keychain access-group entitlements plus the signed CLI Keychain entitlement, and remove obsolete App Group/UDS composition files and settings `depends:ACCESS-IV.1A,ACCESS-IV.1B` `parallel:none`
  - [x] ACCESS-IV.2 Prove by timeout-bounded focused tests that a fake process bound to a stale recorded port receives neither HMAC key nor semantic request body, cannot forge server or response proof, and cannot make the client accept a response; prove that terminating the real App after server-proof verification closes the retained connection and yields `notDispatched` without reconnecting or exposing the body, while loss after complete body transmission yields terminal `outcomeUnknown` without replay; additionally prove loopback-only binding, dynamic-port discovery, Keychain authorization failure, stale-generation replacement, fresh nonce, challenge expiry/single use/replay rejection, body-digest binding, constant-time proof checks before semantic decode, required HTTP framing, 16-MiB/32-connection/header/deadline/cancellation/shutdown ceilings, process-lifetime startup, exact session routing, explicit save, no fallback, and no-retry; then build the actual project-default signed sandboxed App and signed non-sandbox CLI without signing/team/DerivedData command-line overrides, inspect entitlements for zero App Group and exact network/Keychain capabilities, prove no unsigned SwiftPM production executable exists, and demonstrate stopped-App live launch, attach, mutation, immutable readback, unchanged bytes before explicit save, save, termination, restart, and durable CAD-plus-Authored-Mesh recovery through the same App-owned workspace/controller `depends:ACCESS-IV.1` `parallel:none`
  - [x] ACCESS-IV.3 Perform one cumulative source/design/security/behavior review, repair only concrete ACCESS-IV findings, rerun affected focused and actual signed App/CLI proofs once, statically reject application-groups, App Group identifiers/container resolution, AF_UNIX/Unix-socket/same-UID/temporary-root routes, transmitted bearer secrets, unauthenticated HTTP responses, reconnect or retry between challenge and RPC, unsigned SwiftPM production CLI claims, legacy local-project access and CLI local-file mutation/output routes, direct external workspace/controller construction, obsolete compatibility claims, and unrelated dirty-file inclusion, obtain original-designer conformance approval, and commit only the mutually authenticated external HTTP API cutover after confirming branch and commit message `Route external access through the Rupa API` `depends:ACCESS-IV.2` `parallel:none`
- [x] CADAPI-100D The reviewed CADAPI-100 contract requires 100 realized exact-oracle outcomes, including genuine analytic spheres, solely through the signed `rupa` to App-owned `ProjectController` route and is committed as `Require complete CAD API benchmark realization` `depends:CADAPI-D,ACCESS-IV` `parallel:none`
- [x] CADAPI-Q The reviewed design hierarchy fixes one cross-module identity-phase contract: Core stages complete server-generated source/Product identities, semantic `.body` binds a Feature plus body/sheet source-output role, ProjectController owns publication and the exact immutable evaluation snapshot, RupaKit alone projects a requested evaluated `BodyID`, and dry-run/failure return no persistent or evaluated identities; committed as `Clarify semantic CAD identity phases` `depends:CADAPI-100D` `parallel:none`
- [x] CADAPI-I `RupaCore` reports complete immutable server-generated source identity deltas without evaluated topology identity and is committed as `Report generated CAD command identities` `depends:CADAPI-Q` `parallel:none`
- [x] CADAPI-A `RupaAutomation` executes complete immutable prepared source programs with typed server-owned bindings, occurrence-independent native-pattern shape, bounded work, fail-closed Core command admission, and caller-owned group rollback; committed as `Execute prepared Automation programs with typed bindings` `depends:CADAPI-I` `parallel:none`
- [x] CADAPI-F The sole coordinate-free generic semantic registry/compiler now validates and lowers direct and bounded source-only programs with recursive typed values/references, deterministic collision-free prepared slots, complete independent limits and result charge, and no project/publication authority; committed as `Compile bounded semantic CAD programs` `depends:CADAPI-A` `parallel:none`
- [x] CADAPI-W `RupaKit` and `RupaProject` publish compiled semantic programs through one complete five-part project authority and preserve exact committed no-retry evidence; focused Project/RupaKit proof and original-designer review passed, committed as `Publish semantic CAD programs through project authority` `depends:CADAPI-A,CADAPI-F` `parallel:none`
- [x] CADAPI-C The concrete twelve-operation `RupaCADDomain`, exact analytic sphere and ID-free semantic-sketch Core commands, bounded one-step lowerers, typed composition chains, and fixed T12 catalog replay now realize 100/100 existing exact-oracle targets through `ProjectWorkspace` and `ProjectController`; focused Core/CADDomain/Foundation/Automation/kernel tests and original-designer review pass, and the sprint is committed as `Register complete CAD semantic operations` `depends:CADAPI-A,CADAPI-F` `parallel:none`
- [x] CADAPI-P Strict versioned semantic request/result DTOs, five-part authority, bounded full/failure-only response reservations, exact single-use encoding, malformed/over-limit fixtures, and real workspace preview-coordinate advancement are reviewed and committed as `Encode semantic CAD invocation and program requests` plus `Accept advancing semantic preview coordinates` `depends:CADAPI-F` `parallel:none`
- [x] CADAPI-R App composition, Runtime, ProjectAccess, and the signed `rupa` CLI route direct and bounded-program semantic requests through one CADDomain compiler, one exact App-owned authority/lease, and one ProjectController action with reserved single-use responses and separate explicit save; focused Runtime/ProjectAccess/CLI tests, clean App test build and App tests, signed executable syntax/exit fixtures, and design conformance review pass, committed as `Route semantic CAD through project access` `depends:CADAPI-W,CADAPI-C,CADAPI-P` `parallel:none`
- [x] CADAPI-O Exact analytic sphere topology and generic evaluated-primitive measurement preserve complete stable references, exact B-Rep volume, Mesh-only presentation provenance, source/Product identity, invalid-input rejection, and one signed-App published snapshot with 1/8/12/6 topology and 27 stable references; focused tests and original-designer review pass, swift-CAD is committed as `Preserve analytic sphere topology references` (`7430b60`), and RupaKit is committed as `Measure evaluated primitive solids` while all pre-existing dirty files remain unstaged `depends:CADAPI-R` `parallel:none`
- [x] CADAPI-X Production Agent/CLI raw feature-graph authority is removed; typed semantic discovery/direct/program execution now owns the self-contained 100-case path with exact analytic spheres and same-program transform provenance, transaction-local CAD evaluation prevents preview contamination, strict codec/CLI/App/internal 100-case evidence and signed affected live replay pass, the validated `rupa-agent-cad` skill distinguishes current 100/0 evidence from historical 95/5 provenance, original-designer review reports zero P0/P1/P2, and the sprint is committed as `Remove raw Agent feature graph access` `depends:CADAPI-R,CADAPI-O` `parallel:none`
- [x] RUPA-RESP-D Define and commit the complete design-only correction for the current valid multi-body Agent workflow that can generate tens of millions of presentation triangles, build and validate the render plan synchronously on `MainActor`, create a SwiftUI `Path` per triangle, and starve the globally `MainActor` Agent controller: exact B-Rep/source authority must remain unchanged; presentation Mesh and render data must be explicitly derived and resource-bounded; render preparation must be cancellable and off-main; `MainActor` may only publish snapshot-matching UI state atomically; Agent orchestration must remain live without acquiring CAD, project, package, render, or save authority; the sole mutation route remains `ProjectAgentCommandController -> ProjectWorkspace -> ProjectController`; no new coordinator, evaluator factory, project authority, renderer authority, transport, fallback, or compatibility path is introduced; production source and tests are non-goals for this phase, and completion requires a consistent hierarchical DESIGN update, original-designer conformance review, and one coherent design-only commit per affected Git repository. Original-designer review is APPROVE with no P0/P1/P2 findings; static design checks pass; nested `swift-CAD` is committed as `Define bounded tessellation contracts` (`779d227`), and the outer change is committed as `Define responsive presentation boundaries` `depends:CADAPI-X` `parallel:none`
- [x] RESP-B The committed swift-CAD `Package.swift` exposes the `CADCore` library product required by the already committed RupaKit `Package.swift`, so both repositories resolve and build from their own `HEAD` without any uncommitted working-tree change; evidence is a clean-tree `swift build` of RupaKit against the committed swift-CAD revision; committed as swift-CAD `8f2688e` `depends:RUPA-RESP-D` `parallel:RESP-INDEP`
- [x] RESP-DOC The five design defects found in the committed responsive-presentation design are corrected in their owning `DESIGN.md`: `RupaEvaluation` and `RupaCADIntegration` declare the dependency direction that `Package.swift` and the package graph actually enforce; `RupaViewportScene` and `RupaRendering` disclose their direct swift-CAD dependency in `Related Designs`; `RupaUI` discloses the same current-implementation gap that `RupaRendering` already discloses; and `CADIR` discloses that `TessellationLimits` is a target contract with no current implementation. Evidence is direction agreement with `Package.swift`, resolvable links, and the required sections; committed as swift-CAD `bdfb090` and RupaKit `Correct design dependency direction and disclose current gaps` `depends:RUPA-RESP-D` `parallel:RESP-INDEP`
- [ ] RESP-PREV The transient, never-published viewport preview evaluation has one named owner, one bounded contract, and one production implementation that never runs kernel evaluation on `MainActor`
  - [ ] RESP-PREV.1 Confirm from source which existing authority can own an evaluated-but-unpublished document, record the chosen owner with its rejected alternatives, and add the transient-evaluation contract and invariants to the owning `DESIGN.md` before any code change `depends:RESP-DOC` `parallel:none`
  - [ ] RESP-PREV.2 Implement the chosen owner: the viewport prepares each preview document's evaluation off `MainActor` through the existing `EvaluationScheduler`, publishes exactly one `idle`/`preparing`/`ready`/`failed` state per preview revision, discards every stale completion, cancels on revision advance and teardown, and supplies a matching evaluation to the scene builder so the builder's on-demand evaluation is never reached from the viewport preview path `depends:RESP-PREV.1` `parallel:none`
  - [ ] RESP-PREV.3 Make the scene builder able to reject the violating implementation: a caller that owns evaluation selects the supplied-only policy, and the existing on-demand policy remains the unchanged default for every non-viewport caller `depends:RESP-PREV.2` `parallel:none`
  - [ ] RESP-PREV.4 Prove the behavior, not the types: no kernel evaluation occurs on the preview path, a stale completion never reaches rendering, revision advance and teardown cancel the in-flight build, a failed preview evaluation is an explicit typed state rather than an empty successful scene, and the preview document is never rendered without its own evaluation `depends:RESP-PREV.3` `parallel:none`
  - [ ] RESP-PREV.5 Review the change against the owning designs, run the affected focused tests and the RupaRendering/RupaViewportScene builds, and commit the reviewed preview-evaluation ownership `depends:RESP-PREV.4` `parallel:none`
- [ ] RESP-IV All corrected designs, the committed swift-CAD product, and the preview-evaluation implementation compose without contradiction: both repositories build from committed `HEAD`, the corrected dependency directions match `Package.swift`, the disclosed gaps match the actual current implementation, and the affected focused tests pass in one run from a single snapshot `depends:RESP-B,RESP-DOC,RESP-PREV` `parallel:none`
- [ ] RESPI-0 Baseline responsiveness of the current signed build is measured before any performance change, so every later claim has a comparison point: main-thread stall duration, interaction-to-first-frame latency, resident memory, and cancellation latency are recorded for the fixed multi-body fixture against the `RupaRendering` performance-acceptance table `depends:RESP-IV` `parallel:none`
- [ ] RESPI-1 Swift-CAD owns and implements the bounded tessellation contract: `TessellationLimits` exists in source, admission is checked before growth, exhaustion is a typed failure rather than a coarser mesh, and the cache boundary is scoped to the requesting purpose `depends:RESPI-0` `parallel:RESPI-A`
- [ ] RESPI-2 Rupa evaluation is purpose-bound and bounded: each evaluation request declares its purpose and its ceiling, product policy may only lower and never widen the module hard ceilings, and an inadmissible request fails explicitly `depends:RESPI-1` `parallel:none`
- [ ] RESPI-3 Render-plan preparation leaves `MainActor`: the existing plan cache becomes an asynchronous `idle`/`preparing`/`ready`/`failed` lifecycle, construction validates once with no second full traversal, every source vertex is world-transformed at most once into an indexed plan, and the Canvas draws a bounded number of visual-state batches instead of one `Path` per triangle `depends:RESP-PREV,RESPI-2` `parallel:none`
- [ ] RESPI-4 Agent request handling is separated from `MainActor` so an in-flight Agent operation cannot starve window, viewport, or interaction updates `depends:RESPI-0` `parallel:RESPI-A`
- [ ] RESPI-5 The actual signed Rupa application is measured against the same baseline and the performance-acceptance table for the same multi-body fixture: no rainbow cursor, bounded interaction latency, bounded memory, and observable cancellation; every row that still rejects is reported rather than relaxed `depends:RESPI-3,RESPI-4` `parallel:none`
- [ ] CADAPI-IV The original CADAPI-D designer verifies all implementation Sprints compose into one production API that correctly realizes all 100 unchanged T12 targets: simple direct and equivalent one-node requests share one vocabulary/compiler/lowerer, bounded multi-node parameterized/component/native-pattern programs remain compact, analytic spheres are exact CAD, server-generated source bindings and postpublication requested Body identities remain phase-correct through one Workspace-to-Controller publication, every failure preserves atomic/no-retry semantics, explicit save/reload is separate, raw/legacy external authority is absent, P1/P7 performance evidence is measured, and final commit `Verify 100-case composable Agent CAD API` is required with zero P0/P1/P2 findings `depends:CADAPI-100D,CADAPI-Q,CADAPI-I,CADAPI-A,CADAPI-F,CADAPI-W,CADAPI-C,CADAPI-P,CADAPI-R,CADAPI-X,RUPA-RESP-D` `parallel:none`
  - [ ] CADAPI-IV.1 From one fixed task snapshot, run timeout-bounded focused and affected SwiftPM/xcodebuild tests, package compile, success/failure/boundary/cancellation/rollback/postcommit cases, strict codec/golden fixtures, complete registry/lowerer coverage, and a semantic-route replay of all 100 T12 IDs with exact category oracles, exactly 100 realized and zero unsupported/skipped/substitute outcomes; also verify identity phases, generated results, diff check, stale raw-route/document/data scans, and incomplete/Embedded/synchronization/unsafe audits on affected code `depends:CADAPI-100D,CADAPI-Q,CADAPI-I,CADAPI-A,CADAPI-F,CADAPI-W,CADAPI-C,CADAPI-P,CADAPI-R,CADAPI-X,RUPA-RESP-D` `parallel:none`
  - [ ] CADAPI-IV.2 Through the actual signed Rupa App and actual signed `rupa` executable, execute all 100 unchanged T12 cases solely through live `RupaProjectAccess` over authenticated loopback HTTP into the exact App-owned workspace/controller; require one acknowledged semantic direct/program request per case, exact returned bindings and source/B-Rep oracle success, 100 realized and zero unsupported, skipped, direct-controller, raw-graph, fallback, or retry outcomes, while recording exact coordinates and bounded request/compiler/lowering/execution telemetry. Prove mutation leaves disk unchanged until one explicit save, terminate and reopen the same canonical project through `liveProject`, revalidate all retained CAD/presentation results, and prove stale/cancel/limit/lowering/command/evaluation/save/response-loss failures cause no partial state or forbidden retry `depends:CADAPI-IV.1` `parallel:none`
  - [ ] CADAPI-IV.3 Perform one cumulative source/design/behavior review against the committed CADAPI-100 design, repair only concrete scope findings, rerun affected focused evidence and the actual signed 100-case App/CLI run once, verify branch/commit sequence and unrelated dirty state, synchronize DESIGN/PROGRESS/performance status with no 95/5 or legacy-current claim, fold completed CADAPI items with commits, obtain original-designer approval, and commit the final evidence `depends:CADAPI-IV.2` `parallel:none`
- [ ] V8R-0 The V8 redesign contract supersedes the former static-envelope artifact as current acceptance authority while retaining V8-0/T/M/C/V commits only as historical P2 calculation and legacy-route evidence; it rejects the Pillow preview, legacy `DocumentPackageStore` archive, one-angle body placement, valve motion envelopes, and omitted timing drive as completion evidence, consumes the verified RUPA-ACCESS plus CADAPI semantic direct/program route as the sole Agent project-operation boundary, and updates the complete DESIGN hierarchy before implementation; thermodynamic/FEA/CFD/fatigue/durability/emissions/manufacturing-release claims remain explicit non-goals; original-designer review and a coherent local commit are required `depends:CADAPI-IV` `parallel:none`
  - [ ] V8R-0.1 Audit the exact legacy artifact, verified signed-client RUPA-ACCESS live project/session API and explicit-save route, verified CADAPI semantic compiler/CLI routes, App-owned `ProjectWorkspace`/`ProjectController` persistence authority, application load/scene route, current Git state, and all affected design authorities; record a falsifiable route matrix showing that the committed legacy package and Pillow output cannot satisfy the redesign, without modifying unrelated `Package.resolved`, `default.profraw`, or `swift-CAD` worktree state `depends:CADAPI-IV` `parallel:none`
  - [ ] V8R-0.2 Revise the system, RupaKit, CLI/composition, mechanism-module, application, and professional-V8 DESIGN hierarchy so CAD local geometry is source authority, one declarative mechanism graph owns kinematic ratios/phases/limits, a requested crank angle is input state, Product scene transforms are the atomically published pose, evaluation Mesh is derived, and only an actual Rupa application viewport capture may be presented as rendered evidence `depends:V8R-0.1` `parallel:none`
  - [ ] V8R-0.3 Fix the mechanical acceptance contract before code: 90-degree banks with axes at minus/plus 45 degrees, cross-plane throw phases `[0,90,270,180]`, firing order `[L1,R1,L4,R4,R2,L3,R3,L2]` at exact 90-degree firing intervals over 720 crank degrees, eight fixed-length slider-cranks, four-stroke power/exhaust/intake/compression ownership, explicit intake/exhaust opening and closing angles with a bounded continuous lift law, crank-to-cam angular ratio 2:1, equal-pitch timing sprockets/chain, at least one involute external-gear pair with common module/pressure angle and exact pitch-center/phase equations, typed invalid/ambiguous/unreachable failures, and predeclared command/body/triangle/package/time ceilings selected from a measured baseline rather than relaxed to pass `depends:V8R-0.2` `parallel:none`
  - [ ] V8R-0.4 Review the revised contract against the original source paths and reachable counterexamples; prove the V8 workflow consumes only the signed-client RUPA-ACCESS live project/session API and explicit save through the App-owned `ProjectWorkspace`/`ProjectController`, without defining a second project authority, package writer, transport, fallback, or lifecycle policy, then commit only the design/reset artifacts after diff and stale-claim audits pass `depends:V8R-0.3` `parallel:none`
- [ ] V8R-P The professional-V8 workflow proves that the already-reviewed signed-client RUPA-ACCESS live project/session API and CADAPI contracts are sufficient for generic CAD operations through the App-owned workspace/controller and explicit save, without adding a V8-specific access API, semantic operation, alternate controller composition, package-writing authority, raw Automation route, legacy fallback, transport policy, or duplicate lifecycle behavior; exact current-package save/reopen round trips, immutable read evidence, actual App attachment, rollback/cancellation/stale failure, designer review, and one coherent consumer-validation commit are required `depends:V8R-0` `parallel:none`
  - [ ] V8R-P.1 Fix only the V8 consumer semantic-program sequence, required immutable project coordinates and evidence telemetry, resource ceilings, canonical schema-v3 fixture identity, and actual signed-CLI syntax in the professional-V8 design; every operation must use the existing live `ProjectAccessOpening` contract, the App-owned workspace/controller, and registered CADAPI operations, with no new vocabulary, authority, raw graph, alternate local-file access, temporary controller, or V8-specific project-access command `depends:V8R-0` `parallel:none`
  - [ ] V8R-P.2 With Rupa stopped, use the actual signed `rupa` executable to open one fresh canonical schema-v3 project through `liveProject`, resolve the exact App-owned session, apply one bounded atomic `program.execute` request, prove disk bytes remain unchanged before explicit API save, save explicitly, terminate and reopen the same project through the live API, then validate, measure, inspect topology and presentation Mesh; prove exact product/CAD entries, one publication, deterministic state, immutable read coordinates, bounded telemetry, and unchanged source/publication/disk bytes on stale/cancel/compile/lower/command/evaluation/prepublication-save failure `depends:V8R-P.1` `parallel:none`
  - [ ] V8R-P.3 In the freshly restarted actual Rupa application from V8R-P.2, prove a nonempty `ProjectViewSnapshot` and viewport scene at the exact live project/generation/revision/publication/workspace coordinates, apply and read back one generic mutation through the same registered session, prove persistence changes only after explicit API save, terminate and reopen the same canonical project again, and prove no silent legacy, alternate controller, direct writer, alternate local-file path, or fallback occurred `depends:V8R-P.2` `parallel:none`
  - [ ] V8R-P.4 Perform one cumulative consumer-path review, repair every V8R-P finding without changing RUPA-ACCESS authority, rerun the fixed actual CLI/App fixture and focused regression evidence once, audit for incomplete markers and alternate access paths, obtain original-designer approval, and commit only the V8 access-consumer proof `depends:V8R-P.3` `parallel:none`
- [ ] V8R-K A bounded `RupaMechanism` forward-kinematics contract derives every rotating, reciprocating, valve, cam, chain/sprocket, and meshing-gear state from one normalized crank angle without iterative physics or hidden per-pose authority; exact equations, periodicity, phase ownership, limits, typed failures, full-cycle boundary/sweep tests, original-designer review, and one coherent commit are required, while dynamics, combustion flow, flexible bodies, contact stress, lubrication, FEA, and durability remain non-goals `depends:V8R-P` `parallel:none`
  - [ ] V8R-K.1 Add the mechanism module/design and minimal public protocols/value types for named rigid bodies, revolute shafts, fixed-ratio drives, external-gear mesh, chain/sprocket drive, banked slider-cranks, periodic valve-lift laws, pose resolution, validation limits, and typed errors; separate the declarative graph from its solver and the Rupa scene adapter, and forbid direct project/CAD/package ownership `depends:V8R-P` `parallel:none`
  - [ ] V8R-K.2 Implement cross-plane crank and eight piston/rod solutions using the exact bank axes, four throw phases, shared crankpin station geometry, fixed rod length, wrist-pin/crankpin offsets, and a crank reference where the declared firing-order cylinder is at firing TDC; prove finite reachability, rod-length closure, TDC/BDC stroke, 360-degree positional periodicity, and 720-degree cycle indexing across exact boundaries and a fixed full-cycle sweep `depends:V8R-K.1` `parallel:v8r-kinematics`
  - [ ] V8R-K.3 Implement four-stroke cylinder phase and valvetrain resolution so all eight cylinders cover power, exhaust, intake, and compression exactly once per 720 degrees; derive four camshaft angles at half crank speed, continuous bounded intake/exhaust lift with explicit overlap, and zero lift outside declared events; prove firing-order spacing, phase boundaries, 720-degree valve periodicity, maximum lift, and negative timing/cycle cases `depends:V8R-K.1` `parallel:v8r-kinematics`
  - [ ] V8R-K.4 Implement chain/sprocket and involute external-gear constraints so tooth counts/pitch or module/pressure angle determine pitch radii, center distance, speed ratio, direction, backlash/root clearance, and tooth-to-gap phase; prove crank-to-cam 2:1 at all sampled poses, external gear equation `z1*theta1 + z2*theta2 = phaseConstant`, exact 720-degree closure, and typed rejection of incompatible pitch, center, phase, count, or clearance `depends:V8R-K.1` `parallel:v8r-kinematics`
  - [ ] V8R-K.5 Compose the professional-V8 mechanism definition from the three reviewed solvers and run an independent fixed-resolution 0-through-720-degree sweep plus exact event-boundary cases; reject any rod closure error, piston travel violation, piston/valve or rod/crankcase envelope interference, valve overtravel, gear/chain incompatibility, nonfinite transform, duplicate body binding, or unbound moving part, then review and commit the mechanism sprint `depends:V8R-K.2,V8R-K.3,V8R-K.4` `parallel:none`
- [ ] V8R-C The professional V8 CAD is rebuilt as a source-controlled, current-project Rupa CLI assembly with unique semantic moving parts and reviewable engine geometry rather than presentation primitives: cutaway block/heads and gas paths, cross-plane crankshaft, eight pistons/pins/rods, thirty-two valves, four camshafts/lobes, timing sprockets/chains/guides, and a visibly meshing involute gear pair are created only by public Rupa commands; fragments compose atomically, measured source/B-Rep/scene evidence and resource limits pass, all Python CAD/render output is removed from acceptance, and designer review plus one coherent commit are required `depends:V8R-K` `parallel:none`
  - [ ] V8R-C.1 Fix stable semantic IDs, unique body/scene names, local coordinate frames, material/display intent, calculation dependencies, moving/static classification, fragment schema, composition order, and expected source/topology measurements before geometry work; each fragment owns disjoint files and IDs, and no fragment may precompute a pose or write a package directly `depends:V8R-K` `parallel:none`
  - [ ] V8R-C.2 Author the block, bedplate, cylinder/head cutaway, intake runners/plenums, exhaust runners/collectors, valve seats/guides, and required service/interface geometry through bounded Rupa sketch/extrude/revolve/sweep/boolean/fillet operations; prove eight bore axes, bank/pitch/deck dimensions, continuous named intake/exhaust paths, cutaway-only visibility semantics, and absence of geometry presented solely by prose or color `depends:V8R-C.1` `parallel:v8r-cad-fragments`
  - [ ] V8R-C.3 Author the crankshaft journals/throws/webs/counterweights, eight piston crown/skirt/ring-groove and pin interfaces, and eight bored connecting-rod big/small ends with nontrivial reviewable sections through Rupa commands; prove four shared crankpin stations, five mains, correct local frames, eight independent moving assemblies, measured bore/stroke/rod/pin/journal dimensions, and no static-angle placement baked into local CAD `depends:V8R-C.1` `parallel:v8r-cad-fragments`
  - [ ] V8R-C.4 Author thirty-two discrete valves, four shafts with per-cylinder cam lobes, 1:2 timing sprockets, bounded chain/guide/tensioner geometry, and at least one full-tooth involute external-gear pair through Rupa commands; prove tooth counts/profile radii/face widths/pitch centers, cam/valve body bindings, chain path continuity, and exact compatibility with the mechanism definition without claiming contact stress or manufacturing tolerance `depends:V8R-C.1` `parallel:v8r-cad-fragments`
  - [ ] V8R-C.5 With Rupa stopped, open one fresh canonical project through the actual signed `rupa` CLI and the live RUPA-ACCESS API, compose the three disjoint fragments in the exact App-owned workspace/controller, apply source validation in one atomic publication, prove disk bytes remain unchanged until explicit API save, save, terminate, reopen the same canonical project through `liveProject`, and verify semantic coverage, unique bindings, measured critical dimensions, B-Rep topology, nonempty presentation scene, bounded Mesh/package/command telemetry, and absence of `PIL`, `ImageDraw`, custom polygon projection, legacy archive entries, independently rendered preview artifacts, temporary controllers, or direct package-writer use `depends:V8R-C.2,V8R-C.3,V8R-C.4` `parallel:none`
  - [ ] V8R-C.6 Perform one cumulative visual/source review against the engine acceptance contract, repair all scope findings, rerun the exact CLI construction and focused regression evidence once, and commit the rebuilt canonical CAD artifact without retaining contradictory old CAD or documentation as current authority `depends:V8R-C.5` `parallel:none`
- [ ] V8R-POS The mechanism graph is bound to the rebuilt canonical project by exact unique semantic names and a generic Rupa scene adapter; the signed `rupa` CLI resolves a requested crank angle into one complete transform batch and publishes it atomically through the attached live project/session API and App-owned workspace/controller, while persistence occurs only through separate explicit save and pose outputs remain derived evidence; representative firing/event angles and a full analytic cycle prove coherent piston, rod, crank, cam, valve, sprocket/chain, and gear motion with typed rollback, and designer review plus one coherent commit are required `depends:V8R-C` `parallel:none`
  - [ ] V8R-POS.1 Add the generic mechanism-pose CLI request/result and Rupa adapter: resolve every declared body binding exactly once, validate the complete pose before mutation, lower it to one ordered Automation transaction, publish all transforms or none through the exact attached live session, return crank/cam/cylinder/valve/gear telemetry with exact App-owned project coordinates, and expose explicit-save outcome separately without adding a V8-specific CLI command or persistence authority `depends:V8R-C` `parallel:none`
  - [ ] V8R-POS.2 With Rupa stopped, open a fresh canonical base project through the signed CLI `liveProject` API, resolve the exact App-owned session, and use only actual Rupa CLI calls to realize and independently inspect at least crank angles 0, 90, 180, 270, 360, 450, 540, 630, and 720 degrees; prove the declared cylinder reaches firing TDC at every 90-degree step, 0/720 transforms close, pistons reverse correctly at TDC/BDC, rod lengths stay fixed, cams advance exactly half angle, valves match their phase/lift schedule, chain ratios hold, and external teeth remain correctly phased `depends:V8R-POS.1` `parallel:none`
  - [ ] V8R-POS.3 Prove prepublication rejection and unchanged App-owned project/session state and canonical package bytes for missing or duplicate body bindings, stale project/generation/revision/publication/workspace coordinates, invalid crank angle, unreachable slider-crank, incompatible gear/chain geometry, failed clearance/limit validation, evaluation failure, cancellation, timeout, and explicit-save failure; no retry may follow a published mutation and no failure may be rounded to an empty or static success `depends:V8R-POS.2` `parallel:none`
  - [ ] V8R-POS.4 Review the actual CLI process, package mutations, immutable readback, and transform/phase telemetry against the mechanism equations, remove any pose-specific source authority or hidden fixture path, rerun focused Mechanism/Automation/Project/CLI tests and the representative-pose process fixture, then commit the reviewed pose integration `depends:V8R-POS.3` `parallel:none`
- [ ] V8R-APP The canonical CLI-generated V8 package loads in the current Rupa application and is manipulated through the attached Rupa CLI while Rupa remains the viewer: a nonempty fitted viewport displays the engine at multiple crank angles, screenshots are captured only from the actual Rupa window with visible application provenance, scene/pose changes match CLI coordinates and mechanism telemetry, no alert/legacy fallback/custom renderer is involved, and focused App/Viewport verification plus designer review and one coherent evidence commit are required `depends:V8R-POS` `parallel:none`
  - [ ] V8R-APP.1 Build the current Rupa application with the pinned toolchain, open the exact canonical CLI package through `ApplicationProjectCoordinator`, prove successful current-package load, expected file identity, nonempty object registry/evaluation/viewport scene, bounded fit-to-model framing, and no unsupported-package or silent empty-scene state `depends:V8R-POS` `parallel:none`
  - [ ] V8R-APP.2 Attach the real `rupa` CLI to the open application session and apply representative poses spanning power, exhaust, intake, compression, valve overlap, and a full 0/720 closure; after each acknowledged publication, re-read exact app/session coordinates and capture an uncomposited Rupa-window screenshot showing cranktrain, timing drive/gears, and valve motion, with filenames and evidence records bound to package/mechanism/pose hashes `depends:V8R-APP.1` `parallel:none`
  - [ ] V8R-APP.3 Review every retained image and report for provenance and claim scope, reject any Pillow/custom/independent projection or image without a matching Rupa session publication, verify screenshots are presentation evidence rather than CAD/kinematic authority, rerun the focused application load/live-update test, and commit only valid Rupa viewport evidence `depends:V8R-APP.2` `parallel:none`
- [ ] V8R-IV The redesigned V8 passes one cumulative end-to-end verification from actual Rupa CLI creation through canonical package save/reload, mechanism pose publication, immutable CAD/B-Rep/evaluation/scene inspection, current Rupa application load, attached live CLI multi-angle operation, and actual viewport evidence; all contract success/failure/boundary/cancellation/rollback/resource/claim gates, focused and affected-target tests, actual-process tests, package-entry and stale-artifact audits, `FIXME(INCOMPLETE_IMPLEMENTATION)` plus Embedded/synchronization/unsafe audits where affected, diff check, clean task-scope Git review, original-designer conformance review, and final coherent evidence commit must pass with zero P0/P1/P2 scope findings before completion; no production-performance, thermal closure, FEA, fatigue, durability, emissions, safety, or manufacturing-release claim is added `depends:RUPA-VIEW-0,V8R-0,V8R-P,V8R-K,V8R-C,V8R-POS,V8R-APP` `parallel:none`
