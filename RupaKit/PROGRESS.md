# Progress

- [x] T12-0 The Agent CAD benchmark responsibility, design hierarchy, measurement boundary, exactly-100-case envelope, non-goals, invariants, and falsifiable evidence contract are approved, reviewed, and committed as `Define Agent CAD benchmark contract` `depends:none` `parallel:none`
- [ ] T12-A A reusable benchmark contract and deterministic catalog expose exactly 100 stable CAD-basic challenges without exposing oracle answers to candidates, with focused contract tests, designer review, and one coherent commit `depends:T12-0` `parallel:none`
  - [ ] T12-A.1 RupaAgentCADBenchmark defines one major type per file, with candidate-facing public values/protocols in physically separate source files from internal oracle expectations; stable CaseID/category, agent-visible challenge, internal expected geometry, required capability, output-role binding, candidate protocol, bounded Agent action sequence with response-dependent continuation or typed unsupported response, tolerance policy, case outcome, score, and report are defined without allowing public candidate files to reference expectation types; the candidate receives only challenge text, capability metadata, and typed results from its own prior Agent actions, while invalid/non-finite dimensions, duplicate roles, invalid bindings, unsupported-code mismatch, expectation leakage, and catalog drift are typed failures rather than defaults or string matching `depends:T12-0` `parallel:none`
  - [ ] T12-A.2S The sketch catalog owns 52 immutable cases in separate source files: LIN-001...012 exact lines, REC-001...012 exact rectangles, CIR-001...012 exact circles, and ANG-001...016 two-line intersections with specified lengths, shared intersection, orientation convention, and unsigned included angle; values span practical millimeter-to-meter scales, offsets, aspect ratios, axis/quadrant directions, and XY/XZ/YZ planes while remaining safely above the document modeling tolerance `depends:T12-A.1` `parallel:catalog`
  - [ ] T12-A.2D The solid/relationship catalog owns 48 immutable cases in separate source files: BOX-001...012 boxes, CYL-001...008 cylinders, CON-001...008 coincident/parallel/perpendicular/horizontal/vertical/equal-length/concentric/equal-radius sketch relations, TRN-001...008 translated/rotated placements, CMP-001...007 bounded multi-primitive arrangements, and SPH-001...005 required spheres whose current reference capability is explicitly unsupported rather than approximated by a circle, cylinder, polyhedron, Mesh, or direct kernel call `depends:T12-A.1` `parallel:catalog`
  - [ ] T12-A.3 The combined catalog contains exactly 100 unique lexically ordered stable IDs and exactly the declared category counts, exposes deterministic challenge text with dimensions/units/axes but no answer geometry or command hints, retains a versioned public manifest digest, and changes the appropriate public catalog or internal expectation/capability-availability version plus digest whenever an ID, input, expectation, tolerance rule, or capability baseline changes without exposing internal expectation fields through the candidate protocol `depends:T12-A.2S,T12-A.2D` `parallel:none`
  - [ ] T12-A.4 The tolerance contract compares canonical SI source geometry with abs-relative limits derived from the authoritative document ModelingTolerance, rejects values within the kernel degeneracy boundary, uses the document angular tolerance for normalized line relations, and never widens tolerance based on candidate output, tessellated Mesh, display units, or observed error `depends:T12-A.3` `parallel:none`
  - [ ] T12-A.5 Focused tests prove Codable/deterministic value behavior where applicable, candidate/oracle information separation across physical source files and visibility, 100-count/category/ID uniqueness, manifest-digest stability, dimension/role/binding validation, typed unsupported handling, and no success fallback; the original T12 task designer reviews the cumulative T12-A artifact, findings are resolved, all affected focused tests and diff-check pass, and T12-A is committed `depends:T12-A.4` `parallel:none`
- [ ] T12-B A deterministic source-geometry oracle independently falsifies wrong, extra, missing, degenerate, or substitute geometry for every catalog category, with focused adversarial tests, designer review, and one coherent commit `depends:T12-A` `parallel:none`
  - [ ] T12-B.1 The oracle resolves candidate output roles only through the ordered typed responses produced by the runner, including Automation command indexes and returned primary/created FeatureIDs, validates that every binding is current, unique, in range, and owned by the fresh case document, and reads SketchEntitySnapshotService, resolved CAD expressions, MeasurementService, and exact TopologySnapshotService data from the immutable post-execution ProjectViewSnapshot rather than candidate claims, names, renderer triangles, or tessellated bounds `depends:T12-A` `parallel:none`
  - [ ] T12-B.2S The sketch oracle checks exact entity kind/count with no unbound extras; resolved endpoints, centers, radii, widths, heights, planes, lengths, intersections, and angles; non-degeneracy; and the required constraint relations, using order-independent entity matching where the challenge does not prescribe order and preserving oriented versus unsigned angle semantics explicitly `depends:T12-B.1` `parallel:oracle`
  - [ ] T12-B.2D The solid oracle checks exact source feature/body counts with no unbound extras; closed non-degenerate solid kind; box planar topology, dimensions, placement, bounds, and volume; cylinder circular profile, cylindrical/planar topology, radius, axis, depth, placement, bounds, and volume; compound separation/alignment; and a genuine analytic sphere surface/topology/radius/center if sphere capability later becomes available `depends:T12-B.1` `parallel:oracle`
  - [ ] T12-B.3 Every case is binary for realization: all required checks pass or the case fails. Reports preserve per-check expected/observed values and typed outcomes realized, expectedUnsupported, unexpectedUnsupported, invalidSubmission, executionFailure, oracleFailure, timeout, cancellation, or infrastructureFailure; primary completion is realized/100, capability-decision accuracy is reported separately, category scores retain fixed denominators, and an infrastructure failure invalidates the run instead of improving a score `depends:T12-B.2S,T12-B.2D` `parallel:none`
  - [ ] T12-B.4 Adversarial fixtures prove rejection of wrong scale/unit, endpoint, radius, rectangle aspect, box depth, cylinder axis, intersection point/angle, missing/extra entities, duplicate role bindings, open/degenerate solids, circle/cylinder/polyhedron stand-ins for spheres, successful command messages with wrong geometry, and candidate-supplied measurements; failure diagnostics identify the exact violated invariant without mutating the case document `depends:T12-B.3` `parallel:none`
  - [ ] T12-B.5 The original T12 task designer reviews oracle authority, numerical comparisons, topology assumptions, false-positive/false-negative risk, and every adversarial proof against the T12 design; findings are resolved, focused oracle tests and diff-check pass, and T12-B is committed `depends:T12-B.4` `parallel:none`
- [ ] T12-C The benchmark runner executes candidate submissions only through the registered production Agent/project route and supplies a truthful current-capability reference candidate, with isolation/concurrency tests, designer review, and one coherent commit `depends:T12-B` `parallel:none`
  - [ ] T12-C.1 Each case receives a fresh default ProjectController, ProjectWorkspace, registration lease, project ID, generation, transaction revision, publication sequence, and empty CAD authority; the runner gives candidates only the agent-visible challenge and capability snapshot, validates each bounded candidate action, binds the current session/authority coordinates, and executes every mutation, read, and response-dependent continuation through ProjectAgentCommandController before invoking the independent oracle on the final immutable workspace view; immutable public AgentRequest/AutomationCommand payloads may be created as inert action data, but the runner never exposes a direct source-mutation API to candidates; a simple case may use one AutomationBatch, but the contract does not require one batch `depends:T12-B` `parallel:none`
  - [ ] T12-C.2S The reference-plan candidate attempts LIN/REC/CIR/ANG/CON challenges using bounded Agent action sequences and output-role bindings without reading private expectations or mutating/evaluating/constructing a document, kernel, or B-Rep; immutable public request payloads are allowed only as data dispatched through ProjectAgentCommandController, behavioral reachability is established only by the production executor and oracle, and any unsupported or failed operation is retained as an observed outcome rather than rewritten as an assumed success `depends:T12-C.1` `parallel:reference`
  - [ ] T12-C.2D The reference-plan candidate attempts BOX/CYL/TRN/CMP challenges through the same bounded Agent action contract, returns typed unsupported for SPH with the exact structurally absent current capability before any mutation, and never substitutes another primitive; a future genuine sphere realization is accepted only by the unchanged sphere oracle and then requires capability-baseline version advancement, while no non-sphere success count is fixed before execution `depends:T12-C.1` `parallel:reference`
  - [ ] T12-C.3 Execution limits bound Agent action/round-trip count, commands per batch, read records, generated feature/entity count, candidate planning time, case execution time, exact-evaluation work, diagnostics, and report size; cancellation is checked before planning, before each action/publication, and before oracle return; candidate timeout counts as candidate failure, harness failure invalidates the run, and no retry occurs after a published mutation `depends:T12-C.2S,T12-C.2D` `parallel:none`
  - [ ] T12-C.4 Cases share no workspace, controller, package, output file, mutable static fixture, or generated identifier. The scheduler accepts an explicit bounded concurrency value, sorts all final results by CaseID, proves serial and concurrent results identical, and uses measured speedup to select the checked-in default; MainActor/project-actor serialization is reported rather than hidden as parallel execution `depends:T12-C.3` `parallel:none`
  - [ ] T12-C.5 Focused route tests prove successful publication and geometry observation, expected unsupported with zero publication, invalid plan rejection, stale/cancelled prepublication rollback, published no-retry failure classification, per-case isolation, deterministic concurrent ordering, and an architecture boundary scan allowing only immutable public AgentRequest/AutomationCommand payload construction dispatched through ProjectAgentCommandController while rejecting EditorSession.execute, DesignDocument/CADDocument mutation, CADDocumentStore mutation, CAD-kernel evaluation, B-Rep construction, and direct swift-CAD/Mesh operations from candidate/reference-plan sources; the original T12 task designer reviews the cumulative runner, findings are resolved, tests and diff-check pass, and T12-C is committed `depends:T12-C.4` `parallel:none`
- [ ] T12-V The exactly-100-case Swift Testing benchmark, deterministic report, measured runtime envelope, and behavioral regression evidence are complete, reviewed, and committed `depends:T12-C` `parallel:none`
  - [ ] T12-V.1 A parameterized Swift Testing suite exposes all 100 stable CaseIDs as individually attributable cases and runs the reference candidate through the full production route and independent oracle; the first complete valid production run captures the observed per-case ExecutionRegressionBaseline only after the versioned CapabilityAvailabilityBaseline is captured, without preassigning non-sphere success, proves SPH-001...005 are typed expectedUnsupported with zero publication when analytic sphere is unavailable, rejects any infrastructure/oracle-invalid run without establishing or updating a baseline, and subsequent green regression runs require exact environment/catalog/capability digests plus typed per-case outcomes/checks, reporting baselineDrift explicitly rather than treating every benchmark challenge as a required test success `depends:T12-C` `parallel:none`
  - [ ] T12-V.2 The suite emits and round-trips a deterministic machine-readable report containing benchmark/catalog/version/digest, candidate kind, toolchain/platform, the CapabilityAvailabilityBaseline and its digest, the ExecutionRegressionBaseline status/digest when established, per-case and per-category outcomes/checks/timings, observed primary completion and supported-geometry realization, capability-decision accuracy, all named unsupported or failed capabilities, and an explicit statement that the reference-plan score is control-path evidence and not natural-language/LLM reasoning evidence; 95 realized plus 5 expectedUnsupported is recorded only if the executed evidence actually produces that result, and infrastructure-invalid runs never canonicalize failures or update the regression baseline `depends:T12-V.1` `parallel:none`
  - [ ] T12-V.3 Measured serial and bounded-parallel runs record wall time, per-case p50/p95/max, command/feature/topology counts, and timeout/cancellation behavior; the owner selects the smallest concurrency that provides repeatable speedup without shared-state failure, fixes per-case and suite deadlines above the measured envelope with documented margin, and proves timeout guards terminate work rather than leave background tasks or registrations alive `depends:T12-V.2` `parallel:none`
  - [ ] T12-V.4 Timed focused swift-test invocations prove the contract/catalog, sketch oracle, solid oracle, production runner, negative outcomes, all 100 reference cases, deterministic rerun, and registered-session cleanup; no test is marked passed from type shape, command result, evaluation body count, or screenshot alone `depends:T12-V.3` `parallel:none`
  - [ ] T12-V.5 The original T12 task designer reviews the cumulative 100-case behavior, report arithmetic, expected-unsupported semantics, actual production reachability, oracle independence, runtime evidence, and absence of reasoning overclaim; findings are resolved, PROGRESS and design authorities are synchronized, affected builds/tests and diff-check pass, and T12-V is committed `depends:T12-V.4` `parallel:none`
- [ ] T12-IV The original T12 task designer reviews every cumulative artifact and actual execution path against all T12 invariants; exactly 100 stable cases remain traceable, the versioned capability-availability baseline is observed and the evidence-derived execution regression baseline is captured only after a complete valid production run with the five structurally unsupported sphere cases explicit when observed, no non-sphere success assumed, direct-document/kernel/B-Rep shortcuts absent except immutable public AgentRequest/AutomationCommand payloads dispatched through ProjectAgentCommandController, source/oracle/scoring/isolation/resource contracts hold, exact environment/catalog/capability digest comparison reports drift explicitly, infrastructure-invalid runs cannot canonicalize failures, all focused and integrated non-Metal swift tests pass under timeouts, incomplete/synchronization/unsafe conditional audits are classified, commit messages and branch are verified, PROGRESS and the complete DESIGN hierarchy are synchronized, the integration evidence is committed, and the branch is normally pushed to its configured upstream only when it contains no unrelated unpushed work `depends:T12-0,T12-A,T12-B,T12-C,T12-V` `parallel:none`
