# Progress

- [x] DH1 Retain released snapshot-scoped body previews until the successor native frame is applied. Reuse immutable unit-quad collision resources while preserving per-frame handles and retirement. Debug Box rotation native preparation median 53.98ms -> 25.72ms (seven warm updates). Twelve focused Apple-platform tests pass. Revert PF1 parallel shader compilation after a native ShaderLibraryAssetLoader registration crash; discard ineffective parallel spatial/text/path experiments. Implementation commit accompanies this record. `depends:none` `parallel:none`

- [x] PF1 Isolate pan/zoom readout observation from workspace layout and overlap independent native material compilations. Debug mounted-input median pan 19.15ms -> 2.58ms, zoom 3.04ms; first Box native preparation 232.67ms -> 164.67ms. Two UI tests and four native GPU/material tests pass; signed App build/restart verified. User resumed editing after restart; previous three-Box model saved in .verification/CanvasPerformance-20260921.rupa. Implementation commit accompanies this record. `depends:none` `parallel:none`
- [x] PF2 Use chunk-wise GeometryBuffer iteration and direct integer collection operations, reuse validation ID sets, and avoid polygon scratch for existing triangles. Same Debug primitive-add fixture: Sphere evaluation+plan 862ms -> 572ms; Cylinder 885ms -> 665ms. Geometry 98 and CAD 35 tests pass through SwiftPM's generated testing helper; five rendering/GPU integration tests and signed App build pass. Geometry, identities, malformed-input rejection and transaction isolation remain unchanged. Implementation commit accompanies this record. `depends:PF1` `parallel:none`

- [x] GS1 Reduce oversized grid labels from 24pt to 18pt, retaining contrast, full-text bounds and hover continuity. Four native GPU/handoff cases and signed App build pass; restarted App screenshot confirms the adjusted labels. Implementation commit accompanies this record. `depends:none` `parallel:none`

- [x] GL1 Retain mounted camera and grid labels across hover updates (da10f92c). User-requested readability correction uses 24pt semibold and opaque 75% gray; eight native GPU/projection/reuse/failure cases pass and the restarted App visibly displays complete labels. Readability commit accompanies this record. `depends:none` `parallel:none`

- [x] TX1 Remove label line limits and ellipsis policies across Inspector, lists, panels and viewport chrome; wrap long text, grow rows and move header hints outside the fixed-height control row. Nine focused functions (ten cases), signed App build and review pass. Commit c55112b4. `depends:none` `parallel:none`

- [x] GR1 Reduce adaptive grid density to 24pt and retain published ticks during camera readiness waits; implementation 82ecd9b2 passes native projection, hidden/invalid frames, resource identity, scale and GPU text checks (9 functions, 14 cases). Final opacity refinement verified in the rebuilt signed App; companion commit accompanies this record. `depends:none` `parallel:none`

- [x] FE1 Correct source-plane face axes, cap compensation, indexed curved-face highlighting and Inspector dimension mapping. Focused UI and native rendering/gesture regressions pass; implementation commit accompanies this record. `depends:none` `parallel:none`
- [x] ST1 Unify Box/Sphere/Cylinder canvas placement and expose six existing modeling operations through shared palette controls; 16 focused tests (27 expanded cases) pass, including kernel evaluation, rejected inputs and native layout. Implementation commit accompanies this record. `depends:none` `parallel:none`

- [ ] SB1 Clarify Outliner hierarchy and reveal row actions on hover without changing selection, source commands, drag zones, or accessibility. Implemented 18pt indentation/branch guides, root label weight, fixed action slots and hover/VoiceOver/pending visibility; two focused projection tests and signed App build pass. Saved generation-3 sphere document through the App API and restarted the rebuilt executable. Native hierarchy guides and idle action hiding verified; enter/exit and action verification remain pending because concurrent user editing prevented further UI automation. Restart authorization is recorded in AGENTS.md. `depends:none` `parallel:none`

- [x] S1 Product hierarchy lifecycle (group, ungroup, delete) reachable from the workspace. Commit b700eade. `depends:none` `parallel:none`
- [x] S2 Editor commands reachable from the workspace menus and keys: Escape unwinding, selection scope keys, the Tools menu with per-tool prompts, and the answered construction plane key. Commit c3e35355. `depends:S1` `parallel:none`
- [x] S3 What the workspace says reaches the screen, and the canvas stops carrying build notes. Commit 8f6b2fa1. `depends:S1` `parallel:none`
- [x] S4 Workspace chrome: one canvas header above the canvas, and the inspector spends its width on the inspector. Commits 9ef80a92, 11c870bd, f483c413. `depends:S1` `parallel:none`
- [ ] S5 Viewport interaction contract on RealityViewport `depends:S1,S3` `parallel:none`
  - [x] S5.5 Middle mouse orbit, and the keyboard left alone while a field is editing `depends:none` `parallel:none`
  - [x] S5.2 Drag threshold, drag preview origin, and Escape cancellation `depends:none` `parallel:none`
  - [ ] S5.3 Gizmo follows the body and the sketch it moves, proved behaviourally for the sketch `depends:none` `parallel:none`
  - [ ] S5.4 Hover resolution while the pointer moves, and honest axis triad targets `depends:none` `parallel:none`
  - [ ] S5.1 Verification that one affordance-to-command commit path serves every builder `depends:none` `parallel:none`
- [x] S7 Object parameters the Inspector offers reach the canvas. Commits 3900f5cb..85197f92. `depends:S1` `parallel:none`
- [ ] S6 Integration verification of the whole workspace `depends:S1,S2,S3,S4,S5,S7` `parallel:none`
- [x] S8 Remove unused drawing projection exporters and CLI response fields `depends:none` `parallel:none`
- [x] S9 Verify facade/preview removal and consolidated UI display helpers; implementation 13b081fc builds in the signed App and passes six focused Apple-platform tests. `depends:S8` `parallel:none`
- [x] S10 Verify consolidated solid direct-edit behavior and expression resolution; implementation c6e8e514 passes thirteen focused command/expression tests. `depends:S9` `parallel:none`
- [x] S11 Restore benchmark assets with package-private CAD/performance ownership preserved and JSON/CLI/responsiveness tools separated; verified byte restoration, builds, persistence, measurement, and rejection behavior. `depends:none` `parallel:none`
- [x] S12 Restore CADAPI-100 requirements and reconcile relocated design links; frozen catalog/digests and three architecture-boundary tests pass. `depends:S11` `parallel:none`
- [x] R1 Complete sphere source/display integration with unchanged analytic source, oracles and resource ceilings; 46 focused tests and fixed 100-case replay pass, including explicit mesh-exhaustion rollback. Commit a481a5b1. `depends:none` `parallel:none`
- [ ] R2 Verify the mounted viewport and cleanup consumers: preserve concurrent work, execute Apple-platform rendering/input tests and actual App sphere display, resolve scoped failures, review and commit. `depends:R1` `parallel:none`
  - [x] R2.1 Verify native mount, all twelve object gestures and ten profile gesture/stale-frame cases on RealityKit/Metal; remove stale scheme/test selections and verify bounded batching without dropping or reordering cases. `depends:R1` `parallel:none`
  - [ ] R2.2 Signed App verification executed: preserve original in .verification/BeforeSphereVerification-20260921.rupa; create radius-0.75m sphere, confirm visible fit/selection/perspective and Inspector sides64, exact BRep volume 1.7671458676442586, reject negative radius without source mutation, save .verification/SphereVerification-20260921.rupa and reload in a fresh process with the same source feature and exact volume. Remaining observed inconsistency: reloaded document Inspector reports Generated Bodies 0 / Not Evaluated despite visible geometry and successful measurement; root cause and correction are not yet verified. `depends:R2.1` `parallel:none`
- [ ] S13 Complete integration verification: unchanged semantic replay 100/100, tools 121/121, Core 46+13, focused UI 6, native gesture families and signed App build/signature pass; signed-App sphere rendering/persistence pass but reload Inspector status is inconsistent, and the broader Viewport incomplete-verification marker remains. In-process 100/100 is not signed-App 100-case acceptance. `depends:S8,S9,S10,S11,S12,R1,R2` `parallel:none`
- [ ] SB2 Verify sidebar integration: stable row layout, disclosure, selection and action availability in the signed App. `depends:SB1` `parallel:none`
- [x] ST2 Verify toolbar integration: signed App build passes; rebuilt App displays primitive picker, six operation buttons and hover hints; visible Box/Sphere/Cylinder agree with live generation-6 source scene. Existing unrelated workspace verification remains separately tracked. `depends:ST1` `parallel:none`
- [x] FE2 Signed App build and focused Apple-platform tests pass: three-plane evaluated box extents, source-scaled face displacement, native face picking and profile gestures. Live cylinder wall is curved; handle changes radius 1.41m to 1.82m without moving center or height, Undo restores 1.41m with zero failures. User model preserved in .verification/BeforeFaceEditingFix-20260921.rupa. Existing reload evaluation issue R2.2 remains separate; source edit/Undo restored evaluation for this verification. `depends:FE1` `parallel:none`
- [x] GR2 Signed App build passes and native Ortho/Persp views show coarse subdued lines with axis labels, preserving snap 1mm. Camera pan/zoom and label readiness behavior verified by Apple-platform tests. User generation-14 model saved and reopened from BeforeFaceEditingFix-20260921.rupa; original project ID and seven source features retained. Existing reload evaluation issue R2.2 and transient finite-layout startup diagnostic remain outside the grid change. `depends:GR1` `parallel:none`
- [x] TX2 Restarted signed App and reopened the preserved user model. Native screenshot confirms full Document ID, Source Features, Scene Nodes, Generated Bodies, Components, Render Reason, Render Generation, Default Material, Validation Rules and Export Presets labels. Long English/Japanese text wraps at 280/320pt in rendering tests; native minimum-window containment and input/header regressions pass. No explicit numeric line limits, truncation modes or text scale-down remain in app/UI/viewport sources. `depends:TX1` `parallel:none`
- [x] GL2 Integration verified for da10f92c and e124ce83: native camera/projection/section integration passes ten cases; final label GPU, repeat handoff, reuse and projected-grid checks pass eight cases. Signed App rebuild/restart and screenshot confirm legible 24pt ground-plane labels. Unrelated performance measurement remains unfinished. `depends:GL1` `parallel:none`
- [x] PF4 Remove per-triangle uniqueness and boundary hash allocations, preserving edge validation and source winding. Five focused rendering/Metal tests pass, including mixed face topology and malformed triangle rejection. Three-run Debug plan medians: Sphere 112.55ms -> 96.78ms; Cylinder 114.08ms -> 98.13ms. Implementation commit accompanies this record. `depends:PF2` `parallel:none`
- [x] PF5 Retain only the last admitted request's immutable CAD conversion pairs, matched by exact Mesh and source identity; revision caches remain isolated. Six tests prove reuse, invalidation, concurrent reads/replacement, allowance refusal, Undo/Redo and forged-mesh rejection. Three-run mounted addition evaluation median: Cylinder 415.30ms -> 247.95ms; Sphere 390.86ms. No CAD/picking fidelity reduction. Native preparation still costs 127-264ms and the fixture does not measure first displayed pixel. Implementation commit accompanies this record. `depends:PF4` `parallel:none`
- [x] PF3 Integrate ad5de550, bcbe8f4c and 7f2712a3: mounted pan/zoom and primitive-add measurements pass; three native GPU/handoff/collision regressions and signed App build pass. Restarted the 23:27 executable and verified the user's newly created Box renders; preserved active editing instead of reopening the previous five-object model, saved at .verification/PrimitivePerformance-20260921-2304.rupa. Pan input median 2.56ms, zoom 3.10ms; native preparation still 127-264ms, not a first-displayed-pixel measurement. macOS verification only. `depends:PF1,PF2,PF4,PF5` `parallel:none`
- [x] DH2 Native predecessor/successor handoff, real release/failure input, no-op, collision reuse/retirement and cancellation pass; performance fixture and two custom-material GPU tests pass. Signed App build succeeds. Running user's editor is not restarted; the rebuilt app takes effect on next launch. Unrelated dirty files and 58 pre-existing unpublished commits are preserved; no push. `depends:DH1` `parallel:none`
