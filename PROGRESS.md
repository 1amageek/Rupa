# Progress

- [x] RR-1 (completed subitems: RR-1.1, RR-1.2) outer `7c18df41f384d60e5a4334b4b670039591b1e812` + tracked dirty Agent 差分、nested `52c838be52832e49fc93a136eaf4a99ecf871a7f` を固定して応答性修正をレビューした `depends:none` `parallel:none`
- [x] RR-2 既報契約を fixed / remaining に分類し、到達可能な反例と検証限界を確定した `depends:RR-1` `parallel:none`
- [ ] RFX-1 nested Swift-CAD と outer Rupa の独立した corrective path を、exact-source authority、all-or-nothing failure、bounded resource、matching lifecycle、render/picking consistency を保って修正する。Agent dirty差分は直前review済みの同一snapshotとして保持し、新規coding/reviewを行わない `depends:RR-2` `parallel:none`
  - [x] RFX-1.1 CADIR/CADKernel の正本に、effective kernel `MeshArtifactPurpose` と現在limitsによるreuse admission、およびwinding-normal fallbackを含む全output growth前chargeのassume-guaranteeを先に整合させる。Rupa representation purposeの一対一mapping、fidelity変更、別cacheは非目標とする `depends:RR-2` `parallel:RFX-A`
  - [x] RFX-1.2 `DocumentCaches.validateFreshness` はpurpose mismatchをtyped validation errorにする一方、`DocumentEvaluator.evaluate(reusing:)` はpreviousをhintとして扱い、不適合meshだけを再利用せずcompatible exact stateからrequested purposeで再tessellateする。現在limitsはreused/fresh全bodyの累積usageへ適用し、fresh evaluationと同じtyped refusalになることをred/green behavioral testで立証する `depends:RFX-1.1` `parallel:none`
  - [x] RFX-1.3 既存`Tessellating` source compatibilityを保つrequest overloadへ現在`TessellationLimits`とreused meshのchecked aggregate `TessellationUsage`を渡し、production `MeshTessellator`は両preflight/emission budgetへreserved usageを最初にchargeしてからchanged meshをallocation/reserve/appendする。legacy/injected conformerは最終aggregate guardで検証し、reused-only、reused+changed、injected output、boundary-plus-one、内部cancellation、失敗後再評価同一性をdesign・実装・behavioral testで立証する `depends:RFX-1.2` `parallel:none`
  - [x] RFX-1.4 固定toolchainのCADIR/CADKernel focused testsとaffected Swift-CAD testsをtimeout付きで一度通し、designer review後にnested `main`へscheme-only dirtyを除外したscoped commitを作る。root progressは次のouter coding commitへ同梱する `depends:RFX-1.3` `parallel:none`
  - [x] RFX-1.5 現行Canvas/plan/Metal実装と実測を根拠に、既存immutable indexed plan/provenanceを共有し、surface fillだけnative Metal rasterのdepth test・face-normal lightingで描き、Canvasをgrid/寸法/操作overlayに限定する最小設計へRupaRendering/Appのowner・snapshot lifetime・GPU/CPU上限・typed failureを先に更新する。既存identity-picking compute renderer、同期readback、別renderer abstractionは流用・新設しない `depends:RR-2` `parallel:RFX-A`
  - [x] RFX-1.11 preview cacheだけが実Taskを所有しrevision更新・clear・teardownでcancelし、matching completionだけをpublishしてstale success/failureを捨てることをcache lifecycle testで立証する。Viewport/renderer sourceは変更しない `depends:RR-2` `parallel:RFX-A`
  - [x] RFX-1.6 同一planのindexed trianglesをnative Metal rasterでdepth-correctに描き、bounded face-normal tonal shading、外形/feature contour、hover/selection contrast、section-plane fragment clipを実装する。同じproduction GPU buffer/draw pathをbaseline/signpostが測り、Geometry ownerが検証したtriangulation limitsとface ceilingを含め、index/position/triangle/provenance/GPU buffer全storageをreserve/growth前にchargeする。equal-depthはCPU pickingと同じscene-order tie ruleとする。snapshot preparation failureとview/layout/GPU frame failureを分離し、後者はvalid planを破棄せずmatching rendererだけに表示し、次の成功で解消する。Metal shader/pipeline生成失敗もtyped `gpuFailure`へ変換する。published sceneのsurface fillを旧per-triangle Canvasへ重複送信せず、drag previewはrequestのexact target sceneNodeID、編集ghostは`editedBodies`だけをtransient Canvasへ送る既存Viewport内部predicateで絞り、他のpublished bodyの再流入をfocused testで拒否する。同じViewport編集でRFX-1.11のmatching typed preview failure overlayも接続し、published documentを維持する。未実行rowは`notMeasured`とし、構造から成功を推定しない `depends:RFX-1.5,RFX-1.11` `parallel:none`
  - [x] RFX-1.7 actual Apple GPU/device/shader/libraryの`xcodebuild test`で隠面・濃淡・輪郭・section・render/picking一致を立証する。24-copy real planと各boundary-plus-one/overflow/cancellationを拒否し、既存12-body/6,284-segment dense fixtureと512-item/16-segment cheap-body fixtureの成功最大+25%から各count ceilingを、byte ceilingはさらに204.8 MiB以下として確定する。10 post-warm-up runsでproduction native surfaceのCPU encodeとGPU completion、plan readinessを記録し、checked retained/working各204.8 MiB以下とactual in-flight worker cancellation 100 ms以下を立証する。live Observation publication、Canvas overlay、完成frame/input、RSSは`notMeasured`としRFX-6.2のみが受入を所有する。designer review後にrenderer、RFX-1.11 preview cache、baseline、対応design/testsだけを一つのcompilableなouter scoped commitにする。nested CAD commit `db8b801` を前提とする `depends:RFX-1.6` `parallel:none`
  - [ ] RFX-1.8 RupaEvaluation/RupaCADIntegration/RupaKit designに、kernel mesh countからID/topology/index/attributeを含むuniversal storageをchecked preflightするowner、remaining allowance、provider work前・境界・return前cancellation、failure時cache非公開を先に整合させる。上位representation purposeは既存の同一kernel artifact purposeを共有できる `depends:RR-2` `parallel:RFX-A`
  - [ ] RFX-1.9 pre-cancelled empty project、provider/reference/result境界cancellation、複数CAD sourceで先行source charge後の残量だけが次kernelへ届くこと、universal byte boundary-plus-one、overflow、cache非公開を実経路のred/green testで立証する `depends:RFX-1.8` `parallel:none`
  - [ ] RFX-1.10 予測使用量をallocation前にchargeし、同一provider requestの各CAD source評価直前にchecked remaining kernel limitsを導出して渡す。materialization後のactual usage再検証とengine aggregate chargeを残した最小実装を行い、focused/affected testsをtimeout付きで一度通してdesigner review後にevaluation/adapter/design/testsだけをouter scoped commitする `depends:RFX-1.9` `parallel:none`
  - [ ] RFX-1.12 preview cacheのmatching failed/ready stateがRFX-1.6で接続済みのViewport UIへ一意に反映され、failure時はpublished documentを維持し、preview geometry/pickingを不一致にしないことをcombined cache/UI behavioral testで立証する。新たなViewport source編集やrenderer rebuildは行わない `depends:RFX-1.7,RFX-1.11` `parallel:none`
  - [ ] RFX-1.13 RFX-1.10のevaluation cancellationとcommitted preview lifecycleを統合し、actual previewのrevision replacement/clear/teardownでobservable work停止が100 ms以下、late completion非公開、failure可視を既存focused evidenceで立証する。追加source編集・renderer rebuild・別commitは行わない `depends:RFX-1.10,RFX-1.12` `parallel:none`
- [ ] RFX-6 nested/outer exact commitsを統合し、ユーザーが開いているprojectを置換・変更・保存せず、独立検証projectまたは既存標準fixtureで実App・視覚・resource・cancellation・Agent authorityを立証する `depends:RFX-1` `parallel:none`
  - [ ] RFX-6.1 affected Swift-CAD/RupaKit testsを固定snapshot・timeout付きで一度通し、直前review済みAgent隔離/lease testsを統合前提として再実行し、Package.resolved・`DesignDocument+Surface.swift`・既存`RupaKit/PROGRESS.md`・scheme-only変更ほかuser変更がtask commitへ混入していないことを確認する `depends:RFX-1` `parallel:none`
  - [ ] RFX-6.2 signed Release Rupaを独立fixtureで起動し、同一Agent API/workspace authorityからsurface shading/contoursとpicking、UI/run-loop、10-run frame/memory/cancellation、capability/status、typed failureを実経路で確認する。save/restartは検証projectだけへ行う `depends:RFX-6.1` `parallel:none`
  - [ ] RFX-6.3 branch/upstreamとtask外unpushed commitを確認し、root progressと再現可能な証拠をouter最終commitへ含める。設定済みupstreamがありtask外commitを含まない場合だけ通常pushし、tag/release/deployは行わない `depends:RFX-6.2` `parallel:none`
