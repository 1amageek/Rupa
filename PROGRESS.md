# Progress
- [x] INSPECTOR-COMPACT-1 Shared scalar/custom-slider rows and horizontal XYZ fields; compact idle formatting preserves full editing precision and frozen submission mapping. Focused tests passed. Commit: feat: compact inspector controls with shared custom sliders. `depends:none` `parallel:none`
- [x] CANVAS-RECOVERY-1 World curves use one native mesh and position/byte budgets; limits unchanged. GPU Ortho/Persp 1025-point updates, 32-curve/grid producer and body regression passed. App built and restarted after save; new user project visibly renders grid/polygon/handles. Old saved project was not reopened because user resumed editing. Commit: fix: batch sketch world lines within viewport budgets. `depends:none` `parallel:none`
- [x] AFFORDANCE-UNIFY-1 Reused cube producer/input for object transforms, with XYZ arrows/rotation, centered scale, fixed-opposite face/corner resize and zero-extent filtering. Box source edits and dedicated source editing retained. Native/logic checks passed; legacy sketch-only callback explicitly deprecated. Commit: fix: unify object affordance appearance and transform behavior. `depends:CANVAS-RECOVERY-1` `parallel:none`
- [x] CANVAS-ERROR-1 Routed source/render/preview failures to existing Logs, removed canvas error text, preserved failure gates. Hidden-host transition/recovery test and app build passed. Commit: fix: route viewport failures to workspace logs. `depends:none` `parallel:none`
- [x] GEOMETRY-FEEDBACK-1 White/yellow edit outlines, neutral guides, callback-gated handles, and native sketch placement/handle synchronization with cube publication handoff. Native lifecycle/GPU tests and color checks passed; app build passed. Commit: fix: synchronize sketch transform feedback and clarify edit handles. Individual point/tangent source-edit preview remains GEOMETRY-EDIT-3. `depends:none` `parallel:none`
- [ ] GEOMETRY-EDIT-1 User approved free XYZ path editing. Preserve planar sketch constraints and cube/object transforms; add an explicit spatial path source, then fix source identity, preview, arc/polygon/path editing contracts. `depends:none` `parallel:none`
- [ ] GEOMETRY-EDIT-2 Implement missing source editing semantics and transactions; verify geometry, identity, cancellation and Undo/Redo; commit. `depends:GEOMETRY-EDIT-1` `parallel:none`
- [ ] GEOMETRY-EDIT-3 Connect dedicated edit entry, handles and numeric controls to those transactions; verify native input and live generated results without foreground automation; commit. `depends:GEOMETRY-EDIT-2` `parallel:none`
- [x] AFFORDANCE-CONCEPT-1 Defined shared cube control grammar and requirements for cube, rectangle, regular polygon, circle, arc and spline; generated concept board without source changes. `depends:none` `parallel:none`
- [x] CANVAS-CHROME-1 Navigation isolation: 53354fb6c; zero-size startup capture fixed and verified in `fix: defer camera capture until viewport size is valid`. `depends:none` `parallel:none`
- [x] CANVAS-STABLE-1 Retained camera scale independent of geometry; focused control and mounted native projection passed. Commit 33b4a3861. `depends:none` `parallel:none`
- [x] CANVAS-STABLE-2 Stable native tick text and host-owned label root; 6 mount/GPU runs and 5 resource/remount/lifecycle runs passed, including zero blank native frames during geometry replacement. Commit 6c017ad8e. `depends:CANVAS-STABLE-1` `parallel:none`

- [x] ALL-HANDLES-1 Exact screen-radius native hit footprints and mounted-frame test coordinates; all axis stations, six faces/eight corners, crossing and lifecycle verified. Commit c9ece3633. `depends:none` `parallel:none`
- [x] HANDLE-CROSS-1 Signed crossing, positive CAD dimensions and fixed-face geometry verified; native GPU, 42 geometry cases and Workspace Undo/Redo passed. Commit eeb99a744. `depends:none` `parallel:none`
- [x] HANDLE-SYNC-1 Same-update solid/handles/colliders and cancellation verified on orthographic/perspective native cameras. Commit 7002a6829. Two pre-existing centerScaleY edge-hit cases remain outside this change. `depends:HANDLE-CROSS-1` `parallel:none`

- [x] SOLID-PREVIEW-1 Shaded affine preview, source isolation and release handoff: commit 7a7de6cf8; 12 GPU/logic runs passed. Mesh outer Y-marker probes fail with preview disabled too; original checks retained. `depends:none` `parallel:none`

- [x] AFFORDANCE-WIDTH-1 Rendered transform axes/arcs at 3pt with shared native geometry; preserved placement, identity and 7–8pt collision tolerances. Native mounted test passed all 4 projection/line variants and measured 3pt across zooms 0.2/1/20; /tmp/rupa-thick-affordance-final.xcresult. `depends:none` `parallel:none`

- [x] CORNER-3 Repaired spherical interiors and shorter triangle diagonals; swift-CAD 3762b6f. Subdivisions 1/3/5/50 chord-error and radial-normal checks plus resource/provenance regression: 23 passed; native RealityKit test: 1 passed. `depends:none` `parallel:none`
- [ ] CORNER-4 Resolve reference-video low-subdivision silhouette versus CAD-face-preserving display: video Sides=1 contracts to an inner cube, current display retains exact face boundaries. User question pending; do not claim video parity. `depends:CORNER-3` `parallel:none`

- [x] CORNER-1 User confirmed exact CAD Corner and display-only Corner Sides. Preserve source authority, dimensions, placement, radius-zero reversibility, persistence and Undo. `depends:none` `parallel:none`
- [x] CORNER-K1 Exact all-edge box fillet, volume, bounds, mesh, persistence and invalid-radius checks passed; swift-CAD `bf27eef`. `depends:CORNER-1` `parallel:none`
- [x] CORNER-2 Connected exact Corner, display-only Corner Sides, source dimensions and Inspector bounds; root commit 7cfe446f5, swift-CAD quality commit 3595ded. Core 2 tests, project publication 1 test and hidden native rounded-corner test passed. `depends:CORNER-K1` `parallel:none`

- [x] CENTER-HANDLE-1 Native marker priority prevents shaft translation; XYZ centered displacement and native regressions passed (12 functions/33 runs). Commit 9bb674471. `depends:none` `parallel:none`

- [x] AFFORDANCE-1 Anchored box source resizing, six face/eight corner handles, translation cones and centered scale; fixed-face/affine/source-geometry/native-input/Undo verification passed. Commit fd1c05b06. `depends:none` `parallel:none`

- [x] HANDLE-HANDOFF-1 Preserve released object-handle preview through asynchronous source publication; commit 21552dee8. Delayed success/failure/no-op/replacement, duplicate refusal and native translation/rotation/scaling verified: 10 functions/15 runs in /tmp/rupa-handle-handoff-focused.xcresult and 5 functions/11 runs in /tmp/rupa-handle-handoff-native.xcresult. `depends:none` `parallel:none`

- [x] INSPECTOR-FRAME-1 Prevent continuous preparation starvation; commit f7fc4bc77; corrected RED and 8 native focused tests prove 1,000-request coalescing, progress before input settles and stale-query refusal (/tmp/rupa-frame-progress-green.xcresult). `depends:none` `parallel:none`

- [x] INSPECTOR-INPUT-1 Unified Inspector numeric interaction, frozen mapping, latest-request acknowledgement, bounded ordered submission, integer input and parameter submission exclusion. Commit f5d42abaa. `depends:none` `parallel:none`
- [x] SSOT-1 Unified Canvas/Inspector placement admission, occurrence Center and Core source Size/radius; removed legacy Inspector scene evaluation and resize compensation. Parent/shear, shared source, all axes, hidden/locked/invalid targets and Undo/Redo verified by 3 new test functions (4 runs) plus 8 existing tests. Commit 24e34433f. `depends:none` `parallel:none`
- [x] INSPECTOR-LATENCY Replaced deferred transforms with live latest-snapshot intents and bounded consecutive pending edits; 1,000 pending values execute once, component/barrier order, real Workspace final values, failure and Undo/Redo verified. Seven focused macOS tests passed in /tmp/rupa-inspector-coalescing-20260917.xcresult and /tmp/rupa-inspector-sequencer-regression-20260917.xcresult; App build and strict signature verification passed. Scoped commit accompanies this record; running App was not restarted. `depends:none` `parallel:none`
- [x] INSPECTOR-LATENCY-2 Overlapped independent per-group SDK waits and used the synchronous wrapper for initialized LowLevelMesh buffers. Warm selected-body preparation fell from 226-233 ms to 30-33 ms in matched fixtures; separate pulse run measured 4.1-4.4 ms warm MainActor gaps. Native GPU/collision/cancel regression: 20 functions passed in /tmp/rupa-inspector-native-regression-20260917.xcresult; final measurement passed in /tmp/rupa-inspector-stage-final-20260917.xcresult. Signed App rebuilt. No input-to-display or 60-fps guarantee claimed. Scoped commit accompanies this record. `depends:INSPECTOR-LATENCY` `parallel:none`
- [ ] INSPECTOR-LIVE-1 Replace deferred transform drafts with immediate component intents resolved against the latest serialized Workspace snapshot; verify all nine components, reset, multi-selection, retained shear, failure and Undo with real publication. `depends:none` `parallel:none`
- [ ] INSPECTOR-LIVE-2 Audit every Inspector property owner and connect missing live updates; correct source/occurrence center and size semantics without double transformation. Record property-to-command-to-render evidence, including document, shape, sketch, surface, plane, pattern, material and definitions controls. `depends:INSPECTOR-LIVE-1` `parallel:none`
- [x] UI-ACCEPT-1 Current-worktree Debug App built after one bounded timeout and incremental continuation; strict deep signature verification passed, blank Untitled workspace and Model menu inspected. Binary identity and warning limitations recorded in UI_TEST_REVIEW.md. No user project opened. `depends:none` `parallel:none`
- [ ] UI-ACCEPT-2 Resume general UI acceptance after the user-prioritized complete Inspector live-update task. Direct operation authorized; Box Preview/Apply, selection, Fit and Y-only translation/Undo observed. Shape Center Y doubled the placement delta and Inspector transforms required manual Preview/Apply; these are unresolved Inspector findings, not passing UI acceptance. `depends:UI-ACCEPT-1,INSPECTOR-LIVE-2` `parallel:none`

- [x] UI-REPLACE-1 Replaced foreground UI automation with hidden native/GPU contracts and explicit automated/manual ownership for all 47 retired scenarios; fixed stale Mount root removal. Commit 223fe8be. `depends:none` `parallel:none`

- [x] UI-QUIET-1 Six findings recorded in RupaKit/Tests/UI_TEST_REVIEW.md; replaced routine screen automation with explicit non-window contract selection, removed screen takeover, aligned Loft refusal and rejected zero-test false greens. Integrated script passed 29 UI and 6 rendering test functions with no failures/skips; scoped commit includes implementation and evidence. Visual inspection remains separate, not inferred from these tests. `depends:none` `parallel:none`
- [x] UI-NORMAL-1 Inspector properties use one transaction with busy refusal, Undo/Redo and atomic failure, verified through production bindings and Workspace; commit 1d4c0d3b. Native activation remains an integration check. `depends:none` `parallel:none`
- [x] UI-NORMAL-2 User-approved affine inspector retains shear through nine numeric edits, world-axis scale after rotation, reflection and gimbal lock; perspective/singular failures stay explicit. Seven focused macOS tests passed; scoped commit records implementation, App activation remains in integration. `depends:UI-NORMAL-1` `parallel:none`
- [x] UI-NORMAL-3 CAD and authored-Mesh presentations share occurrence-owned placement handles and source-edge previews; native gestures, cancellation, immutable assets, Undo/Redo and incomplete-selection refusal verified in 23 macOS cases; commit 64717bd6. Remaining App UI routes belong to UI-NORMAL-IV. `depends:UI-NORMAL-2` `parallel:none`
- [x] UI-WIRING-0917 Reviewed CAD/Mesh draft, body placement and inspector production paths; found world-axis scaling versus TRS inspector incompatibility and per-node multi-selection transactions, and confirmed authored-mesh gizmo exclusion. Source unchanged; whole-UI runtime acceptance is not established. `depends:none` `parallel:none`
- [x] UI-P1-0917 Fixed persistent body translation/rotation/axis scaling and atomic group placement, occurrence-owned mesh previews, release-point measurement, Escape and stale source/selection/parent-frame refusal; reviewed and verified task-only changes in commit 6d439eba. New topology editing and P2 capability expansion remain out of scope. `depends:none` `parallel:none`

- [x] UI-REVIEW-0917 Reviewed body affordance production routing, placement, cancellation, instance preview and test assertions at bcdde529 plus the current UI worktree; correction inventory distinguishes source-confirmed defects from the historical axis defect and unverified runtime behavior. Focused xcodebuild exited 65 before tests because Metal Toolchain is unavailable; no runtime pass or whole-application completeness claimed. `depends:none` `parallel:none`

- [x] UII-1 要求されたCAD/Mesh UI gapについて、実装・caller・behavioral testからexact backend capability、authority、failure、preview/commit/cancel契約を確定し、API宣言だけをsupportedと扱わない `depends:none` `parallel:none`
  - [x] UII-1.1 file/historyを確認し、STL/OBJはAuthored Meshとして編集可能、STEP readerは存在するがexact B-rep source retentionが未接続、AppがURLを所有しCore provenanceへabsolute pathを残さない、metadataがないSTL/OBJ importは明示unit必須、STEP unitは保持、unsupported entityはtyped refusal、historyはdependency-safe reorder/suppression/parameter stageとundoが必要と確定した `depends:none` `parallel:UII-DISCOVERY`
  - [x] UII-1.2 CAD/Mesh workflowを確認し、Revolve/Loft/Booleanを含むCAD commandsと評価、bounded Mesh edit/Make Editable、Project preview/commitは実在する一方、`ProjectSourcePreviewResult`がstaged evaluationのrender payloadを捨てること、Mesh pickingはpresentation provenanceのsource face/vertex IDを使うこと、source feature参照はoccurrence transformを含まないことを確定した `depends:none` `parallel:UII-DISCOVERY`
  - [x] UII-1.3 Core/CADSemantic/ProjectBridgeの既存契約に合わせ`Transform3D.Matrix4x4`をrow-major、translation indices 3/7/11として標準化し、TRS decomposition/composeとnon-affine/shear/projective typed refusalを使う。ユーザー判断により旧column-major UI互換・migration・layout推測を作らず、obsolete helper/sourceだけを削除して保存済みユーザープロジェクトは削除しないと確定した `depends:none` `parallel:none`
  - [x] UII-1.4 UII-1.1〜1.3を既存master/RupaKit/UI設計へ統合し、UIはsource authorityを持たずWorkspace操作だけを送り、Appだけがfile accessを所有し、Core/kernelは確認済みmissing capabilityだけを追加する最終contractをdesignerが承認した。Boolean/Loft等はexact source-frame semanticsを守り、occurrence transformを無視せず、未対応のnonidentity operand/entityはtyped refusalとする `depends:UII-1.1,UII-1.2,UII-1.3` `parallel:none`
- [ ] UII-2 Project/Workspace、history/file backend、RupaUI draft、MainView/Renderingの各既存ownerへ必要なgapだけを実装し、source authority、atomic publication、typed failure、preview/commit/cancel、undo/redoを維持する。native workspace配置、発見可能な既存History/Definitions/Parameters、snapshot-boundな4表示mode、mounted viewportだけを対象にするUI/API/MCP camera controlをUI/RenderingとApp control-planeの責務に分けて統合する `depends:none` `parallel:none`
  - [x] UII-2.1 既存Project preview stagingが一度生成したcandidate source/evaluationから非authorityのimmutable render payloadを返し、既存Workspace scene builderへ渡す契約をowning DESIGNへ先に記載して実装する。同じvalidated Mesh supportを再利用するMesh preview render-payload request overloadもこのownerに置く。再評価、ProjectViewSnapshot化、revision publication、新engine/cacheを行わず、CAD/Mesh geometry一致、failure payloadなし、stale replacement/cancel、commit-only publicationをfocused test・designer review後にscoped commitする `depends:UII-1.2` `parallel:UII-IMPLEMENT`
  - [x] UII-2.2 history ownerのDESIGNを先に更新し、suppression、dependency-safe reorder、parameter edit、stale generation、atomic stage、undo/redoを既存source commandとして実装・focused test・designer review後にscoped commitする `depends:UII-1.1` `parallel:UII-IMPLEMENT`
  - [x] UII-2.3 MainView/Renderingの単一ownerが、既存EditorCommandと確定済みpreview contractだけを受けるCAD creation draft planner/view、単位付きnumeric Fillet/Chamfer、row-major XYZ rotation/position/scale、presentation provenanceによるAuthored Mesh element edit/Make Editable componentを一つのcoherent UI pathとして設計・実装する。obsolete column-major helperを削除し、non-affine/shear/projective、source/occurrence不一致、invalid selectionをtyped refusalする。Meshのsource-wide validation・face/vertex ID検索をMainActorのpick/draft経路で反復せず、既存Workspace/Core validationまたはoff-mainのsnapshot-bound projectionへ置く。local draft、exact parameter/ID forwarding、preview replacement/cancel、Apply一回をtestし、history/file UIと新authorityは含めない `depends:UII-1.2` `parallel:UII-IMPLEMENT`
  - [x] UII-2.4 format/Core/App ownerのDESIGNを先に更新し、STEPはreaderが受理したexact entity subsetとembedded unitsを保持する編集可能なexact CAD source、STL/OBJは明示unitまたはformat metadataを持つAuthored Meshとしてimportする。既存reader/writerとApp file-panel boundaryを使い、loss/unsupported/cancel、temporary output、atomic publication/undo、sanitized format/fingerprint provenanceをfixture testsで立証し、designer review後のscoped commit対象を完了した。GE baseline 13 pass、scale-invariant修正後のlarge-scale normal 14/14 pass（`/tmp/rupa-realitykit.5ZnrUm/ge-normal-green.log`）、App isolated coordinator/Agent tests 3/3 pass・xcodebuild exit 0（`/tmp/rupa-realitykit.5ZnrUm/ge-app-isolated-tests.log`）。App production compile/signingもpassし、検証用bundle IDでLaunchServicesの既存App再利用を回避、user-installed Rupa PID 38558は未変更。既存AgentRequestの`execute`→`setParameterExpression`修正を含み、scoped commit `af367e31`。fidelity文書hunkはscope外として除外した `depends:UII-2.2` `parallel:UII-INDEPENDENT`
  - [x] UII-2.5 UII-2.3 UI/Rendering の対象限定レビューを完了し、UI commit `d9527b93` と本 commit の canonical translation fixtures / cancellation-owned worker 修正を対象に、CPU 397/397 tests pass を確認した（GPU/view lifecycle は UII-3 全体 gate）。 `depends:UII-2.3` `parallel:none`
  - [ ] UII-2.6 UII-2.1〜2.5のtyped contractsを一人のMainView ownerが既存toolbar/menu/inspector/sidebarへ統合し、CAD/Mesh source authorityをUIへ渡さないselection→parameters→preview→commit/cancel workflowとして接続する。source/occurrence refusal、operation catalog、selection gating、Mesh IDs、preview replacement/cancel、Apply一回、history dependency refusal、STEP/STL/OBJ routing、failure visibility、keyboard/undoをtestし、designer review後にscoped commitする `depends:UII-2.1,UII-2.2,UII-2.4,UII-2.5` `parallel:none`
  - [x] UII-2.7 RupaRenderingの4表示mode、source-face provenance境界、off-main admission、bounded draw pass、depth/section/picking不変を本commitへ実装し、Xcode 411件中409件の初回結果とtest-only修正後の対象12/12、4modeのApple GPU pixel/depth/clip/normal、CPU admission/draw-bound証拠およびdesigner reviewを完了した。live frame/RSS/latencyはUII-3.2が統合検証する `depends:UII-1` `parallel:UII-VIEW`
  - [x] UII-2.8 RupaUI/ModelingのNavigationSplitView、Scene/History sidebar、Properties/Definitions inspector、4mode forwarding、selection/draft/accessibility/authority不変を本commitへ実装し、UI27件・Core history2件の29件とSwiftPM build-test productsを通過させ、designer reviewを完了した `depends:UII-1` `parallel:UII-VIEW`
  - [ ] UII-2.9 「Blenderのようなdynamic definitions」を、既存named CAD parameters + dependency-safe feature/modifier historyへの式binding拡張とするか、Geometry Nodes相当のgraph authoring/runtimeまで含めるかをユーザーの明示回答で確定する。回答までcreation式semantics、graph source API、node editor、互換layerは設計・実装せず、UII-2.7/2.8の表示・発見性作業は独立して進める `depends:none` `parallel:none`
  - [ ] UII-2.10 Rendering/UI ownerがowning DESIGNを先に更新し、document-lifetimeの`@MainActor` viewport controllerへ既存camera、orbit basis、axis、transition、4display modeの一時状態を集約する。UI gestureとAPIは同一storageへ`fitVisible`、`fitSelected`、degree orbit、point pan、factor zoom、isometric/xFront/yFront/zFront、orientationを保つcamera reset、4mode変更を適用する。fitはprecomputed visible worldBoundsをO(1)、visible selected occurrenceだけをselection変更時のO(items)で集約し、8 cornersを現在のparallel basis/actual fitting rectへ収め、ruler、source、Undo、persistenceを変えない。空、nonfinite、clampで収納不能はtyped refusalとし、MainViewのfit buttonも同じcommandを使う。state適用とdraw完了を区別し、solver/controller/UI tests、designer review、scoped commitで閉じる。現時点はSwiftPM build-tests、UI label 2、`render-final-tests.log`のcontroller 3（same-ID remount含む）+ Apple GPU 2の計5件がpassし、parameterized solverは今回filter対象外のため以前の証拠を維持する。App build-for-testingと署名verifyもpassし、Logs開閉後のFit無効化は独立fixtureのlive確認で解消したが、Fit後の右端偏り・clipとcommitが未完了 `depends:UII-1` `parallel:UII-INDEPENDENT`
  - [ ] UII-2.11 AgentProtocol/MCP/CLI ownerがowning DESIGNを先に更新し、Rendering依存のないFoundation値の`AgentViewportOperation`/`AgentViewportState`と`viewport.list/state/execute`をstrict codecへ追加する。state/executeはsession IDとmounted viewport UUIDを必須とし、executeは任意のexpected viewport revisionでUI操作とのstale raceを無変更で拒否する。MCPは`rupa_list_viewports`、`rupa_get_viewport_state`、`rupa_execute_viewport`だけを公開し、既存`ProjectAccessSession.send`/`CLIService.send`を再利用する。numeric/unknown-key/byte bounds、codec、MCP dispatch、CLI forwarding tests、designer review、scoped commitで閉じる。現時点はMCP 5・CLI 2・codec 3がpassし、review修正後のMCP 5・codec 1もpassしたが、commitは未完了 `depends:UII-1` `parallel:UII-INDEPENDENT`
  - [ ] UII-2.13 Core ownerが`RupaCore/DESIGN.md`のobject naming契約に従い、通常SceneNode用`renameSceneNode`とComponentInstance用`renameComponentInstance`だけをEditorCommand→CADDocumentStore/EditorSessionへ実装する。通常nodeはID/hierarchy/reference/object/transform/visibility/lock/material/geometry/CAD feature名を保ち、component instanceは既存unique nameを検証してreferenceまたはobject descriptorが同instanceを指す全SceneNode名と原子的に同期する。`constructionPlaneID`付きnodeは既存`renameConstructionPlane`、generic constructionは通常node、Pattern Array rootは既存`updatePatternArray(name:)`を使い、生成outputと誤ったowner経路はtyped refusalにする。正規化同名かつ全mirror一致はgeneration/evaluation/Undoなしのno-op、同名でもmirror不一致なら一つの修復mutationとする。成功/no-op/repair/empty/duplicate/missing/generated/stale、atomic rollback、1-step undo/redo、bridge表示名をfocused test・一度のdesigner review・scoped commitで立証する。既存visibility/lock command群とsource transaction以外のbatch engineは追加しない `depends:UII-1.4` `parallel:UII-OUTLINER`
  - [ ] UII-2.14 RupaUI ownerが`RupaUI/Outliner/DESIGN.md`を正本として、native SwiftUIの独立Outliner componentとMainView adapterを実装する。stable SceneNode hierarchy/orderをdense single-line disclosure treeで示し、document-lifetimeのexpand/collapse、ancestor-preserving name searchとall/visible/hidden/locked/unlocked filter、multi-selection、F2/Return/Escape/context-menu rename、Show/Hide/Lock/Unlock、exact Isolate、Show All、current-selection Frameを提供する。Core naming ownerと既存commandをcurrent snapshotから一つのsource transactionへ変換し、生成Pattern outputの個別操作は理由付きで拒否、Component Definitions/Assetsはread-only、instanceの重複editable listは作らない。MainActorでCAD/Mesh/evaluationを走らせず、duplicate/delete/reparent/new collectionや新UI frameworkを加えない。projection/action/component tests、一度のdesigner review、scoped commitで立証する。現時点は対象14件とWorkspace統合2件、および独立fixtureでrename/cancel/failure/undo/redo/search/disclosure/batch/row action/filter/isolate/show-allのlive操作がpassし、scoped commit待ち `depends:UII-1.4` `parallel:UII-OUTLINER`
  - [ ] UII-2.15 UII-2.13/2.14を固定snapshotで統合し、Workspace実経路でrename同期、batch全成功/一件失敗rollback、stale拒否、undo/redo、filter中のancestor/disclosure、selection-aware context action、rename keyboard、current-selection Frameを確認する。source authority、Pattern Array ownership、既存viewport session、user dirtyの非混入をdesignerが一度だけ確認し、task-control更新を該当scoped commitへ含める。Outliner固有のlive経路とLogs開閉後のFit有効化は確認済みだが、正しいworld bounds `min(-0.03,-0.052,0)` / `max(2.252,0.052,0.45)`に対するFit後のgeometry右端偏り・clipだけが残るため未完了 `depends:UII-2.13,UII-2.14` `parallel:none`
  - [x] UII-2.16 Core ownerが`RupaCore/DESIGN.md`へProduct hierarchy移動契約を先に記載し、`moveSceneNodes(ids:parentID:beforeSiblingID:)`を一つのtyped `EditorCommand`として実装する。選択中の最上位subtreeだけを現在のscene orderで一括移動し、`beforeSiblingID`はfilter上のindexでなく移動先parentの実source sibling IDとする。parent `nil`はroot listを表し、same-parent reorderは既存`localTransform`のbit patternを維持する。reparentは移動前world transformを保持する新local transformをrow-major `parentWorld.inverse * oldWorld`で求め、nonfinite/noninvertible、missing/stale/empty、self・descendant cycle、不正anchor、locked、generated Pattern output、source-owned targetを全変更なしのtyped failureにする。IDs、descendant構造、geometry、feature history、名前、visibility、materialを維持し、1 transaction・1 Undo、成功/no-op/reorder/reparent/multi-root/refusal/rollback/undo-redoをCore focused testsと一度のdesigner reviewで立証してsource-complete handoffする。Project bridgeのworld-occurrence保持はparentがUII-2.19で検証し、commitも同じcoherent integration境界が所有する。既存consumed-profile用`nestSceneNode`は汎用moveへ流用せず、duplicate/delete/new collectionを追加しない `depends:UII-1.4` `parallel:UII-DND-SHADING`
  - [x] UII-2.17 Rendering ownerが`RupaRendering/DESIGN.md`へephemeral viewport shading契約を先に記載し、document-lifetimeの`ViewportControlSession`へ表示modeと独立した`Studio`/`MatCap`/`Flat`、studio light rotation、specular toggle、single/material/deterministic-random solid color、theme/custom background、wire-color preset、backface cullingを保持して既存View→MTK rendererへ渡す。Studioは既存depth-lit surface、Flatはlightingを計算せずbase colorを表示し、MatCapはview-space normalから組込みの小さいbaked sphere textureを2D sampleして、analytic lightingをMatCapと呼ばない。material色は既存Library/`materialID`だけを読み、random色はstable occurrence identityから決定し、per-object source color authorityを追加しない。Solid系だけがsurface shading/color/specularを使い、Wireframeはwire/background、Normalsは既存signed RGB/backgroundを維持し、無効な組合せはUIで適用対象を明示する。backgroundはgeometry/depthへ影響させず、backface cullingはrenderとscreen hit-testの同じfront-face規則を使う。settings変更はsource/evaluation/history/persistence/plan geometryを変えず、per-frame geometry rebuild・同期readbackを行わない。追加texture/bufferは既存renderer lifetimeで一度生成し成長前に課金する。CPU contract testsとApple GPUのmode/color/light/matcap/cull/depth/section/picking pixels、bounded resource evidence、一度のdesigner reviewで立証してsource-complete handoffする。live frame evidenceとcommitはUII-2.19のcoherent integration境界が所有する。Shadow/Cavity/DoF/XRay、world scene、texture/material editing、偽のdisabled UI、新dependencyは含めない `depends:UII-1.4` `parallel:UII-DND-SHADING`
  - [x] UII-2.18 RupaUI ownerが`RupaUI/Outliner/DESIGN.md`と`RupaUI/DESIGN.md`を先に更新し、Outlinerのnative drag/dropとMainViewのnative shading popoverをUII-2.16/2.17の固定済み公開contractへ接続する。DnD payloadは同じmounted Outlinerだけが照合できるprivate nonce、captured selected IDs、document lifetime/generationだけを運び、external dataをsource commandへ変換しない。drop先はstable parent/`beforeSiblingID`から解決してfilter/展開状態のrow indexを使わず、stale・別window/document・external・read-only/generated・不正targetは理由付きで拒否し、MainViewが既存Workspaceへ一つのmove transactionを送る。shading panelは既存native popover/grouped controlsから同じ`ViewportControlSession`だけを更新し、現在のdisplay modeで意味のないcontrolsを明示的に無効化する。DnD token lifetime/drop resolution/selection-root/filtered anchor/refusalと、shading control forwarding/document reset/accessibilityをcomponent testsと一度のdesigner reviewで立証してsource-complete handoffする。source authority、renderer、file/persistence、汎用DnD frameworkをUIへ複製せず、commitはUII-2.19のcoherent integration境界が所有する `depends:UII-1.4` `parallel:UII-DND-SHADING`
  - [ ] UII-2.19 UII-2.16/2.17/2.18を固定snapshotで統合し、Workspace実経路のsame-parent reorder・world-preserving reparent・multi-selection・stale/cycle/generated/locked refusal・atomic rollback・1-step undo/redo、Project bridgeのworld-occurrence保持、同一OutlinerだけのDnDを確認する。4 display modesそれぞれでshading設定の適用範囲、MatCap texture、material/random安定性、background/wire/cull、section/depth/selection/picking一致、resource/frame上限をCPU+Apple GPU testsとsigned Appの独立fixtureで確認し、ユーザーprojectを書き換えない。一度のdesigner総合review、UII-2.16〜2.18のcoherent scoped commitとtask-control更新で閉じ、既存UII-2.15のFit clipおよびUII-2.4/2.6/2.9/2.10〜2.12の未完gateを成功扱いしない。現時点はCore/UI/Project focused tests 27件とRendering Apple GPU/CPU 9件、signed Debug App build・署名検証・独立fixture起動・Viewport Shading button表示、MCP session応答まで確認済み。native UI toolが応答前に`Sky Computer Use native pipe closed before response`で反復切断されたためDnDとshading toggleのsigned App実操作は未検証で、scoped commitも待機中。追加のShadingスライダー修正は連続値化とラベル分離を行い、全9箇所の同種指定を確認（該当はShadingのみ）、native layout・小数角度binding・4modeを含むfocused 3testsがpass（`/tmp/rupa-slider.dFMEQN/tests-final.log`）。この追加修正の起動中Appへの反映と実ドラッグ確認は未実施 `depends:UII-2.16,UII-2.17,UII-2.18` `parallel:none`
- [ ] UII-2.12 App ownerがper-window viewport controllerをApp registryへdocument lifetimeとviewport ID付きで登録・解除し、`ApplicationViewportRequestRouter`がviewport 3methodだけを短いMainActor hopで実viewportへ変換・実行する。既存registered session/lifecycle検証を再利用し、複数windowから1つを推測せず、unmounted、stale viewport revision、wrong session、controllerなしをtyped failureにする。通常status/capabilityはMainActor非依存を維持し、execute成功はstate適用のみを返しdraw完了を主張しない。固定したRendering/wire contractに対するApp sourceとfixtureを独立実装・検証し、multi-window/mount/session/stale/cancel/router tests、designer review、scoped commitで閉じる。型を含む累積統合検証はUII-3が所有する。現時点はApp Debug build-for-testingと`codesign --verify --deep --strict`はpassし、LaunchServices `IDELaunchErrorDomain 20`は解消したが、App hostがtest bundleをloadせず新3testsは120秒timeoutで未実行（`/tmp/rupa-viewport-control.kRKxvl/app-tests.log`）、commitも未完了 `depends:UII-1` `parallel:UII-INDEPENDENT`
- [ ] UII-3 全commitを固定snapshotで統合し、affected package/App testsとsigned App live workflowsを一度ずつ検証して、要求したCAD/Mesh UIと旧応答性gateを実経路で立証する `depends:UII-1,UII-2` `parallel:none`
  - [ ] UII-3.1 affected Core/Geometry/Project/Workspace/UI/App/format tests、RupaRenderingの4mode/fit/controller CPU/Metal tests、Agent codec/MCP/CLI/App viewport route tests、Core object-name/atomic batchとOutliner projection/keyboard/context-action testsをtimeout付きで通し、各operation/format/mode/viewport/Outliner commandが実handlerへ到達することと、user dirty・Package.resolved・既存task artifactがscoped commitsへ混入しないことを確認する `depends:UII-2` `parallel:none`
  - [ ] UII-3.2 signed Release Appを独立fixture/temp filesだけで起動し、creation、numeric edge treatment、Mesh edit/Make Editable、rotation、history/parameter、STEP/STL/OBJ round-tripのsuccess/failure/preview/commit/cancel/undo、Outlinerのtree展開・ancestor付きsearch/filter・inline rename/F2/context menu・multi-selection batch/rollback・isolate/show all・current-selection Frame、Scene/History/Definitions/Parametersの発見性、4modeの視認差・boundary表示・depth/selection/sectionの一致、fit visible/selected button、camera orbit/pan/zoom/orientation/reset、API/MCPのlist/state/executeと複数window/unmounted refusal、input/frame/RSS/memory/cancellationを確認する。API応答のstate applyと実drawは別に観測する。ユーザーprojectは変更・保存しない。旧outer `6d48d4a6`を含むlive acceptance未完了は`RupaKit/Baselines/signed-app-responsiveness-2026-09-05.md`から引き継ぎ、build/testで代替しない。署名済みDebug Appを`/Users/1amageek/Applications/Rupa.app`へ更新し、元のユーザーprojectを変更せず独立213KB・12-body fixtureでLogs開閉後のFitまで確認したが、右端偏り・clipが残り、現在のMac lockによりlive継続、MCP reflection、応答性確認は未完了 `depends:UII-3.1` `parallel:none`
  - [ ] UII-3.3 designerが実装とlive evidenceを一度だけ総合reviewし、root progressを最終commitへ含める。branch/upstreamとtask外unpushed commitを確認し、安全な設定済みupstreamがある場合だけ通常pushする。tag/release/deployは行わない `depends:UII-3.2` `parallel:none`
- [ ] RUPA-CAMERA-S The fixed camera projection contract is implemented by separate lower-projection and UI/API owners, with no shared mutable source outside the agreed Viewport.swift range split; both source leaves complete before fixed-snapshot integration `depends:none` `parallel:none`
  - [x] RUPA-CAMERA-0 The DESIGN hierarchy fixes the default-parallel camera lens enum, Layout-owned optional homogeneous projection/ray, bottom-center control placement, existing SavedView schema mapping, truthful Agent state, ownership boundaries, and falsifiable evidence `depends:none` `parallel:none`
  - [ ] RUPA-CAMERA-P RupaViewportScene/Rendering owner implements the agreed default-parallel ViewportCamera projection enum and Layout-owned optional homogeneous projection/ray contract across SurfaceRenderer, reversed-Z near clipping, CPU/grid/picking, all runtime geometry consumers, session lens state, bounded lens-preserving fit, and camera reconstruction; behind-eye values never mirror, clamp, become NaN, or fall back to parallel, existing axisFront/source/persistence/material behavior is unchanged, and focused CPU plus Apple GPU behavior is reviewed once `depends:RUPA-CAMERA-0` `parallel:RUPA-CAMERA-SOURCE`
  - [ ] RUPA-CAMERA-UI UI/Agent/App owner uses only the RUPA-CAMERA-0 public seams to replace the bottom-center ViewportAxisTriad static labels with working orientation, Ortho/Persp, and Reset Pan and Zoom controls, round-trip the existing SavedView orthographic-height or perspective-FOV/distance schema without lens loss, and expose truthful parallel/perspective, exact optional FOV, and setProjection through strict Agent/MCP/App state and execution tests without new camera or project authority `depends:RUPA-CAMERA-0` `parallel:RUPA-CAMERA-SOURCE`
- [ ] RUPA-CAMERA-IV On one fixed snapshot, verify parallel compatibility and perspective foreshortening/near clip/depth/grid/selection/picking, independent orientation/lens/reset/fit, SavedView round-trip, and API/MCP state/execute with focused CPU, Apple GPU, and signed-App fixture evidence; perform one designer review, preserve the pre-existing dirty/staged real index and user project, create only the coherent scoped commit, and do not promote older UII pending gates to complete; evidence: Apple targeted 40/40, all 112 distinct Rendering tests, 15 real-GPU rendering tests, Debug App build, strict deep code-sign verification, and built-app orientation/projection control markers passed; pending: native live clicks because the UI pipe is unavailable, replacement of the currently running old App, App-hosted tests, and scoped commit `depends:RUPA-CAMERA-P,RUPA-CAMERA-UI` `parallel:none`
- [ ] RUPA-WIRE Wireframe and solid-with-boundaries render source-face boundaries as continuous solid lines by applying triangle-only reversed-Z depth bias before the boundary pass and restoring zero bias for line draws; preserve all other shading, picking, section, resource, camera, and user-state contracts; evidence: the real-GPU red fixture exposed 12 omissions, the fixed 16-test Apple GPU suite passed, the App build and strict deep code-sign verification passed, and the installed App showed continuous cylinder boundaries after reopen, Fit Visible, Wireframe, body selection, and Fit Selected while MCP stayed generation 0/clean and the fixture file plus real index hashes remained unchanged; pending: inclusion in the dependent camera commit `depends:RUPA-CAMERA-IV` `parallel:none`
- [x] RUPA-SIDEBAR-ICONS-D RupaUI and Outliner DESIGN own one compact icon contract: actual Scene Outliner, History, Definitions, and Assets icons use semantic monochrome SF Symbols in a shared 20x20-point slot with 13-point regular/medium type, while Outliner alone owns the fixed group/Body/Mesh/Sketch/Feature/Construction/Instance/Pattern mapping; existing row height, depth indent, disclosure glyph, generated badge, colors, states, and interactions remain unchanged, with no new component, framework, or state owner `depends:none` `parallel:none`
- [ ] RUPA-SIDEBAR-ICONS-S RupaUI implements the design through one root native SwiftUI `WorkspaceSidebarSymbol` adapter and updates only the icons actually rendered by Scene Outliner, History, Definitions, and Assets; symbol choice derives from existing Outliner row semantics and preserves selection, hidden, disabled, name/F2, drag-and-drop, visibility, and lock behavior; focused source inspection proves no parallel icon-sizing policy or unrelated sidebar behavior change; the fixed-snapshot design-conformance review passed with 0 findings, and the scoped `RupaUIPackageTests.xcscheme` follows the existing native test-scheme format; the coherent commit remains pending on the pre-existing uncommitted Outliner/camera source and there is no upstream `depends:RUPA-SIDEBAR-ICONS-D` `parallel:none`
- [ ] RUPA-SIDEBAR-ICONS-IV On one fixed snapshot, focused RupaUI tests prove every required kind mapping, 20x20 slot, 13-point monochrome rendering, retained disclosure/badge sizing, and unchanged interaction/state callbacks; one designer review plus successful App build and signed-App live inspection confirm aligned recognizable icons in Scene, History, Definitions, and Assets, preserve user files/index, and defer the coherent commit when the pre-existing Outliner dependency is not yet committable without expanding earlier task gates; `xcodebuild test-without-building` passed the exact 14 tests, including 120 native render cases (20 SF Symbols x 2 schemes x 3 colors) at 40x40 px @2x with nonempty unclipped glyphs (`/tmp/rupa-sidebar-icons.TzAU0e/tests-final.xcresult`), and the App build plus strict deep codesign passed (`/tmp/rupa-sidebar-icons.TzAU0e/app-build.log`); live inspection remains blocked because CUA confirmed the Mac is locked and cannot unlock it, so the running/installed App has not been quit or replaced and remains old; fixture content and the real staged index (`fa87b52c55323fdb69349c4fbd4c7b05eebc41c0c51ed522394d060bbe7390c9`) remain unchanged `depends:RUPA-SIDEBAR-ICONS-S` `parallel:none`
- [ ] RUPA-INSPECTOR-COMPACT RupaUI uses a native HSplitView bounded by the allocated detail region, gives surplus width to Canvas, starts Inspector at 320 pt, and shares 8 pt horizontal insets; evidence: initial size, native frame containment, settled divider resize retained after window resize, and unchanged workspace authority passed in 2 focused tests (/tmp/rupa-inspector.vbeJDK/settled-layout-tests.xcresult); App build and strict deep codesign passed, installed App restarted successfully with multi-body-cylinder-assembly, Properties/Definitions and compact layout verified visually, project file SHA unchanged; the native inspector modifier that caused startup constraint cycling was removed; pending: coherent scoped commit without absorbing earlier uncommitted dependencies (no upstream configured) `depends:none` `parallel:none`
- [x] RUPA-CANVAS-TOOLS-D RupaUI and Modeling DESIGN fixed the original 18-entry route contract for the 11 left and 7 right tools. Its clicked-object Measure meaning is superseded by RUPA-MEASURE; the other routes retain their real next input, existing Workspace/draft result, truthful help/state, typed refusal and cancel path, including Surface as sheet Loft, Mesh as element editing without a Logs substitute, Section at the clicked origin/current plane, and all seven right destinations. UI owns only transient intent/navigation/result presentation, Workspace remains source authority, and no CAD kernel, graph editor, render preparation, coordinator, dependency, or broad redesign is added `depends:none` `parallel:none`
- [ ] RUPA-CANVAS-TOOLS-S One executor implements the remaining original routes in `WorkspaceToolPalette`, `WorkspaceUtilityRailCompactView`, `WorkspaceCanvasCommandPlanner`, `MainView`, the two existing RupaRendering face/edge accessibility-marker actions, and their focused test owners: preserve working Select/Sketch/Polygon/Arc/Spline/Solid/Sweep command paths, route Surface to sheet-Loft preview/apply/cancel, keep the Surface and Mesh operation roots at the allocated split height while retaining distinct child accessibility identifiers, route every native-pointer or marker Mesh input through the same ready presentation/domain/provenance pick before Authored Mesh element edit or Make Editable guidance, preserve Section click position/orientation through an existing construction-plane command, and use native expanded-rail section IDs for all seven compact destinations without ancestor accessibility-ID replacement. The original clicked-object Measure implementation is historical and RUPA-MEASURE exclusively owns its removal and replacement. A fixed behavior matrix proves each remaining action's success, applicable invalid/precondition refusal, cancel transition and mutation/nonmutation boundary; source edits add no render work to MainActor and are handed off without staging or committing unrelated dirty state. Evidence to date: final source fixes and designer review are complete; signed App build/codesign passed, scoped RupaUI tests passed, all seven compact destinations and Solid/Sketch/Polygon/Arc/Spline/Section live paths passed, and Surface and Mesh workflows reached their native forms. Leave open pending valid Loft/Sweep live gates and coherent integration. `depends:RUPA-CANVAS-TOOLS-D` `parallel:none`
- [ ] RUPA-CANVAS-TOOLS-IV On the fixed snapshot, run the scoped RupaUI/App tests and one designer conformance review, then build and strictly sign the App and operate the 10 non-Measure left tools plus all 7 compact destinations in the signed App using only isolated fixtures; RUPA-MEASURE-IV exclusively owns Measure acceptance. Live evidence must distinguish local activation, input/settings, visible result, refusal and cancellation, prove the known Solid path remains connected, verify Surface opens a full-height sheet-Loft form whose missing-profile refusal leaves Apply disabled and Cancel reachable, prove each right button lands on its named section with its own accessibility identifier, and preserve the original project SHA, Workspace source authority, staged index and unrelated files. Create only the coherent scoped commit including task-control updates; if the required pre-existing uncommitted UI dependency cannot be committed without absorbing unrelated scope, leave this item incomplete and report that exact gate. No push occurs because no upstream is configured. Evidence to date: App build and strict deep codesign passed (`/tmp/rupa-toolbars.w7DACX/app-build-integration.log`); final RupaUI tests passed (`/tmp/rupa-toolbars.w7DACX/ui-integration.xcresult`), Rendering tests including an actual Metal frame passed (`/tmp/rupa-toolbars.w7DACX/render-tests.xcresult`), and live right-rail/left-tool evidence passed for the recorded routes. Still incomplete: verify valid Loft and Sweep workflows, restore the original project, install/run the final build, then create the coherent commit; the Mac is currently locked and no unlock response has arrived. `depends:RUPA-CANVAS-TOOLS-D,RUPA-CANVAS-TOOLS-S` `parallel:none`
- [ ] RUPA-ORBIT-S One coherent camera sprint adds an optional bootstrap `ViewportCamera.focus` that the mounted session resolves once to a finite world point; `ViewportLayout` keeps `renderOrigin` only for numerical precision while projection and rays use the resolved focus. Pan point deltas are atomically canonicalized through the active view plane into focus and clear their temporary screen offset; Fit Visible/Selected assign the bounds center before solving both lenses; orbit/orientation preserve focus and current visible height through zoom compensation within the existing camera limits; anchored zoom and saved-frame restore retain their exact finite focus. The existing strict Agent state and App adapter expose focus xyz without a new operation, controller, source authority, persistence format, or geometry-plan rebuild. Focused CPU tests must reproduce and close Fit/pan-to-orbit drift in parallel and perspective, prove bounds independence, saved-view round-trip, finite atomic refusal, and unchanged geometry identity/selection/dirty state; one existing real-Metal projection/picking test must prove shifted-focus CPU/GPU agreement `depends:none` `parallel:none`
- [ ] RUPA-ORBIT-IV On one fixed snapshot, the designer reviews only RUPA-ORBIT-S against the owning focus contracts, then timeout-bounded focused Rendering/Protocol/App tests, one real-Metal shifted-focus path, signed App build/code-sign, and an isolated live fixture prove three-finger pan/Fit then orbit stays centered in Ortho and Persp and the existing API/MCP pan-orbit path returns and applies the same finite focus. Preserve the open user project, its dirty/selection state, the existing index, and unrelated files; observe state application separately from the displayed GPU frame, do not promote prior pending UII/camera/live gates, and follow the repository's existing scoped commit/push constraints. Evidence to date: the fixed-snapshot designer review has no remaining finding; focused Rendering tests passed 8/8 including the real-Metal shifted-focus path (`/tmp/rupa-orbit-fix.2iSoDH/package-focused.xcresult`), Agent codec tests passed 3/3 (`/tmp/rupa-orbit-fix.2iSoDH/agent-focused.xcresult`), and the final App build plus strict deep code-sign passed under `/tmp/rupa-orbit-fix.2iSoDH`; the CLI build and strict deep code-sign passed (`/tmp/rupa-orbit-fix.2iSoDH/cli-build.log`), and that built CLI completed MCP `initialize` then `rupa_list_sessions` against the unchanged running old-App session `13BDAD7E` with `dirty:true` and authority counts `7/10/22/0`; the staged index remains at SHA `3658f1aa...`. Still incomplete: the Mac is locked, so live focus operations and the displayed GPU result are unverified; approval to save and restart the unsaved `Toolbar-CAD.rupa` project remains unanswered; App-hosted tests are unexecuted; and the scoped commit is blocked by existing uncommitted dependencies with no configured upstream `depends:RUPA-ORBIT-S` `parallel:none`
- [ ] RUPA-GLASS The bottom camera strip now uses one native GlassEffectContainer with glass buttons and an interactive axis disk, semantic tint plus a checkmark for lens selection, and unchanged compact layout, camera callbacks, and source authority. Evidence: projection callback/no-duplicate-command and input-exclusion tests passed 2/2 (`/tmp/rupa-glass.0JERVF/final.xcresult`); final App build and strict deep code-sign passed; installed and built debug dylib SHA match (`2ae14426797851a4ef756b3c35d6895241931c7784403f76c0a66425884f9d74`); the signed App was restarted only after confirming an empty clean project, and real Ortho/Persp, X/Y/Z, Isometric, orientation menu, reset, selected-state accessibility, and dark-mode pointer/material behavior were observed. The unsuccessful in-process SwiftUI accessibility test harness was removed; native actions were verified in the signed App instead. Light/high-contrast appearances were not visually verified. No project source data was changed, and the pre-existing index SHA remains `3658f1aa46a2ae01ad246041e26b4f50f82737fb2e71d856f0003fd26e43514f`. Pending: a coherent commit without absorbing the pre-existing uncommitted projection dependencies; the branch has no upstream. `depends:none` `parallel:none`
- [ ] RUPA-LOGS RupaUI owns Logs pane visibility as user-controlled transient presentation state: keep the existing initial collapsed default and any explicit construction/restoration input, permit only the existing header and toolbar controls to open or close the pane, and remove every source-commit, diagnostics, tool, preview, status, and error route that programmatically reveals it. Diagnostic storage, transient status, operation success/failure, source mutation, selection, Undo, and public/domain APIs remain unchanged; collapsing does not clear diagnostics and reopening displays the retained values. Focused tests prove representative success and failure/status routes append the same diagnostics without changing visibility, and the signed-App Canvas test creates Box without an auto-open or Canvas-height loss, confirms the status diagnostic remains available, then manually opens and closes Logs through both intended controls. A task_reviewer approves the fixed implementation and evidence, and only a coherent scoped commit is created without absorbing the pending RK migration or user-staged index. Evidence to date: the signed App warning/Logs/diagnostic-retention/Box/hover-and-inspector-width scenario passed with exit 0 (`/tmp/rupa-realitykit.5ZnrUm/rupa-logs-manual-app.log`; `/tmp/rupa-realitykit.5ZnrUm/rupa-logs-manual-app.xcresult`). `depends:none` `parallel:none`
- [ ] RUPA-MEASURE RupaRendering owns one transient ViewportMeasurement component and MainView only selects/presents it: replace the old clicked-object summary with click start, live current-pointer preview, click end, finite 3D Euclidean world distance and a noninteractive projected dimension line; the same resolver uses current snap provenance, then a matching ready visible-surface hit, then the actual camera ray on the explicit effective plane, with no stale-hover commit or guessed depth. Ordinary object selection separately draws at most three `World bounds` X/Y/Z rulers for exactly one unambiguous occurrence, using bounded outside candidate placement that avoids the projected object, accepted labels and canvas chrome or omits an unsafe ruler. Tool/Escape/snapshot cancellation is synchronous; source, selection, persistence, Undo, `MeasurementAnnotation`, existing `MeasurementService`, camera authority and MCP remain unchanged. Implement and focused-test the component plus MainView wiring, run one designer conformance review, and create only a coherent scoped commit if existing dirty dependencies can be isolated; otherwise record the exact commit gate. The one targeted re-review confirmed all five fixed-snapshot findings resolved: explicit depth is independent of snap enablement; snap failure falls through only to an explicit surface or plane with a visible warning; leader, chrome, object and cross-ruler label collisions are rejected; all World X/Y/Z values remain in Rendering-owned status with omitted rulers identified; and interaction suppression plus Measuring/Measured presentation follow the actual phase. Focused Rendering/Input/Metal verification passed 14 tests (`/tmp/rupa-measure.lFz4qP/render-final.log`). A subsequent signed-App Box check exposed ancestor-coordinate contamination in chrome exclusions; the isolated native host fixture reproduced five failures with an outer 80-point offset (`overlay-red.log`), moving the existing named coordinate-space modifier after the complete overlay stack removed that offset, and the local-coordinate regression plus narrow-palette native test passed 2/2 (`overlay-green.log`). The final signed App with debug dylib SHA-256 `f2d69ef33c613d9321f823cf9b38ccef37770b83c92420e7e15b80bab65368b3` is installed and live Box verification now shows X/Z rulers and leaders clear of both the utility rail and object, with Y explicitly omitted and all X/Y/Z values retained in status; only the coherent scoped commit prerequisite remains open for this item. `depends:none` `parallel:none`
- [ ] RUPA-TOOLS-AUDIT On one fixed snapshot, inspect source routes and behavioral tests for the 10 non-Measure left tools, 7 right utilities, 10 Model-menu operations and 5 Mesh operations; RUPA-MEASURE and final integration exclusively own the eleventh left tool. Exercise isolated signed-App success where a bounded fixture exists and at least activation, truthful precondition/refusal and cancel otherwise. Report each operation as GUI-success verified, lower-layer-only verified, activation/refusal-only verified, broken, or unverified; distinguish missing coverage from a defect, include source/selection/dirty/Undo observations, and consider discoverability and CAD/Mesh workflow fitness without changing production code or expanding this task to repair independent findings `depends:none` `parallel:none`
- [ ] RUPA-MEASURE-IV On one fixed integrated snapshot after RUPA-MEASURE and RUPA-TOOLS-AUDIT, run timeout-bounded focused Rendering/RupaUI tests, one Apple-platform Canvas/input path, App build and strict deep code-sign verification. Install/restart only after preserving any open user state, then use the current clean empty project plus an isolated object fixture to prove live two-click/hover measurement in Ortho and Persp, snap provenance, world-distance invariance under camera change, selected-object ruler collision/hit-test behavior, Escape/tool/snapshot invalidation, and unchanged source/selection/Undo/dirty/project bytes. The original designer reviews the actual implementation and audit evidence once; commit only scoped coherent files after checking branch/index/prerequisites, normally push only if no task-external unpushed commit and a safe upstream exists, and do not tag/release/deploy. Current evidence: the final App build succeeded and strict deep code-sign passed (`app-integrated.log`), producing debug dylib SHA-256 `f2d69ef33c613d9321f823cf9b38ccef37770b83c92420e7e15b80bab65368b3`; the local-coordinate overlay tests passed 2/2, native pointer forwarding passed 1/1, and camera regressions passed 2/2 (`overlay-green.log`, `input-camera.log`, `camera-regression.log`). That exact signed App is now installed and running: final live checks show Box X/Z labels and leaders clear of the utility rail and object, Y explicitly omitted while all X/Y/Z values remain in status, a surface two-click distance of 819.89 mm, and a Perspective explicit-XY empty-space two-click distance of 988.867 mm with 2D snap disabled; Escape followed by Select restored bounds and preserved the Box selection. The isolated fixture SHA-256 remains `579cd4bc0e1ac68bc30b3bdc6a9d398114687d10df911bdb6834efe20bbe479f`. This item remains open only for the exact native-hover/Undo evidence gaps and the coherent commit, which awaits the user's decision about pre-existing dirty dependencies; the branch has no upstream. The independent Saved View defect observed on an earlier build was not retested against this final binary and is not promoted to a final-build result. `depends:RUPA-MEASURE,RUPA-TOOLS-AUDIT` `parallel:none`
- [x] RK-1 RealityKitをRupa viewportの唯一のtarget world-rendering backendとするsystem/package/module/component contractをmacOS 27+前提で確定し、現行productionがSwiftUI `Canvas` + `ViewportSurfaceView` + custom Metal/identity rendererであることと、RK-2〜RK-5/RK-IV後のRealityKit-only stateを全階層で区別した。CAD kernel/history/stable ID、ProjectWorkspace/MCP authority、`ViewportControlSession`、SwiftUI nonspatial chromeを維持し、world-known geometry、bounded camera-relative annotation、camera-only no-resource-generation、matching frame/pick tuple、stale-root detachment、native API isolation/cancellationの責務を固定した。section clippingはnative `ClippingComponent` hierarchyが所有し、custom material discard/pre-clipped replacement Meshをfallbackにしない。`RealityViewportFrameDescriptor`は必須public typeでなくdesign notationとし、minimal seamを`RealityViewport.prepare(plan:) async throws -> @MainActor RealityViewport` + `RealityViewportView`、既存cache request/snapshot identity、既存session viewport revisionとした。Apple-GPU native runtime testは1/1・0.766 sでbox/line/text/Path/camera/collision triangle identityに成功（`/tmp/rupa-realitykit.5ZnrUm/native-runtime-final.log`とxcresult）、hosted `RealityView` probeはOrtho/Persp project/ray/hitTest API実行に成功したが逆投影parityは証明していない（`view-probe.log`）。Path64は約1.033 sでcamera-frame処理には使わず、native generation interrupt、CAD integration、native section/depth、signed Appは未証明として後続itemへ残した。元designerの初回review 4件と対象限定再確認は収束済み。設計差分は既存stagingを変更せず最初のcoherent coding commitへ含める `depends:none` `parallel:none`
- [x] RK-2 RupaRendering ownerが既存のbounded scene/plan lifecycleを再利用してRealityKit surface sceneとproduction Viewport cameraを実装し、source/evaluation/previewを再解釈せず、one current + one pending、cooperative cancellation、matching request identity、visible typed failure、no stale publicationを維持する既存契約を完了した。exact candidate tree `0393b72...` のbuild-for-testingが成功し、`rk2-candidate-tests.log`/xcresultで64/64（42.304 s）、honest exclusion 1/1（0.044 s、xcodebuild exit 0）、diff-check/deinit guard passを確認した。designer `migration_contract`はexact treeを承認し、clearDepth=0の既存greater規則一致・追加findingなし、Agent/MCP/Outliner混入なし、MeasurementはDESIGN変更のみを確認した。RK-2はnative camera/surface基盤までを対象とし、Canvas world overlayはRK-3、legacy identity pickingはRK-4、旧backend/hybrid削除はRK-5、RealityKit-only統合はRK-IVで継続する。本項目の[x]は親のcoherent対応commitへ含める前提であり、そのcommit失敗時は未完了へ戻す。 `depends:RK-1,UII-2.4` `parallel:none`
- [x] RK-3 RealityKit-only production world-overlay migration is complete: all three `Viewport` world-space routes now use the bounded native batch/cache/mount lifecycle, including geometry, grid/axes, annotations, selection, previews, affordances, measurement/rulers, cold-start/remount, camera projection and grid-coverage fixes; native GPU/focused tests and signed-App live Ortho/Isometric/Persp verification passed. Exact candidate tree `9b66e08251b9096943ede88c1ae03e27391da94d` built successfully with macOS 27 deployment entries and strict code-sign (`/tmp/rupa-realitykit.5ZnrUm/rk3-exact-candidate-app-build.log`). `depends:RK-2` `parallel:none`
- [x] RK-AXIS Native mathematical X/Y/Z reference axes now use fixed-capacity RealityKit line/label resources with homogeneous Ortho/Persp frustum clipping, empty-scene independence, camera-only updates, and explicit degenerate/nonintersection disablement. Isolated candidate build passed; native/pure/producer verification passed 6 test definitions/7 executions in 1.195s, and task_reviewer approved the Float/native-plane correction. `depends:RK-3` `parallel:none`
- [x] PERF-DIAG-1 単純な配置のread-only実経路診断を完了した。source変更ではnative surface/resourceを全再生成し、same-snapshot overlayはassetを再利用するがEntity rootを交換する。配置commit前にpersistence encode/validate/reconstruct整合性round-tripがあり、自動選択によりview projectionが二度走り、Viewport bodyのcache missは全scene graphをMainActorで同期構築するが毎frame処理ではない。既存4 tests/1.346sはreuse・off-main plan・releaseだけを、3秒App sampleはidleだけを証明し、配置latencyのms内訳と支配要因は未測定で修正も未実施。task_reviewerは5点がsourceと整合することを確認済み `depends:none` `parallel:none`
- [x] RK-CLEAN-1 Retired obsolete Canvas/Metal viewport hosts and dedicated tests after the RealityKit migration: removed 146 private GraphicsContext drawing functions (excluding live `drawSelectionDragRectangle`), obsolete grid/snap renderers, the old surface host/lifecycle tests, and two dedicated scene tests; net source reduction 6,182 lines with exact deletion and candidate comparison. Verification: build-for-testing exit 0; `/tmp/rupa-realitykit.5ZnrUm/rkclean-native-regressions.log` reports 9 test definitions/16 executions in 26.037s, including native cold/remount, axes/grid failure, resource/material, producer/chrome/input paths; task_reviewer approved with no in-scope findings. Scoped commit message: `refactor: remove retired Canvas and Metal viewport hosts`. App performance improvement was not measured. `depends:RK-3` `parallel:none`
- [x] RK-CLEAN-2 Removed the unused MeshSourcePresentation pick and render wrapper backend that the RealityKit cutover left behind: deleted `MeshSourcePresentationPicker`/`Picking`/`PickIndex`/`PickIdentity`/`PickRecord`, the `MeshSourcePresentationRenderer`/`Rendering` wrappers over `MeshSourcePresentationRenderPlan`, and the unreferenced `MeshSourcePresentationSectionGeometryCache` (8 files, 267 lines); narrowed `MeshSourcePresentationPickError.Code` to the two cases `MeshSourcePresentationNavigationMap` still throws and dropped its dead `occurrenceIDs` accessor. Surviving tests were repointed at `MeshSourcePresentationRenderPlan` and `MeshSourcePresentationNavigationMap` instead of being deleted, so no invariant lost its owner. Reachability was measured at HEAD `2069e5cf`: every deleted symbol had zero production references because the already-committed RK-4 leaves had removed the last call sites. Verification: `xcodebuild build-for-testing` exit 0; serial `xcodebuild test-without-building` over `RupaRenderingTests` reports 681 tests in 18 suites finishing in 168.831s with 680 passed and 1 failed, and the separately filtered `productionMainViewGraphAuditIncludesEveryNonLegacyUIAndPresentationSource()` run reports 1 test passed in 0.348s; the single failure is the pre-existing `viewportHitTesterSelectsBodyInteriorAndSketchEdges()` at `ViewportSceneTests.swift:3142` that fails identically before this change. Scoped commit message: `refactor: remove unused MeshSourcePresentation pick and render wrappers`. `depends:RK-3` `parallel:none`
- [x] RK-PERF-1 Native surface owner reuses exact-equal immutable geometry and material resources across source snapshots within one current owner and one candidate; source changes immediately withdraw old display/query authority, replacement owns fresh provenance and Entities, full geometry comparison stays off MainActor, and existing byte admission includes retained grouping data. Verification: `/tmp/rupa-realitykit.5ZnrUm/rkperf-reuse-final.log` xcodebuild exit 0, 10 test definitions in 19.552 s including native Ortho/Persp cases; the three candidate files match the tested snapshot, while the old source failed the six asset-reuse assertions. Reviewer approved with no in-scope findings; scoped commit message is `fix: reuse immutable RealityKit assets across source updates`. App placement latency is not measured. `depends:PERF-DIAG-1` `parallel:none`
- [x] RK-4 Production pointer, hover, measure, snap, object, Mesh, CAD affordance and rectangle picking now run entirely on RealityKit native queries against the mounted frame. Point routes take a bounded `Scene.raycast(.all)` along the native-projected ray, read the world point from the native hit position, and resolve `triangleHit.faceIndex` through the prepared run lists to a CAD sub-shape identity. Rectangle routes ask a region visibility raster what the same mounted frame drew at every device pixel of the rectangle, reusing that frame's projection, section half-space and back-face rule instead of building a second projection, and answer the face, edge, vertex and occurrence scopes from it; the sketch entity, sketch region, curve and surface handle families are answered from the same frame's projection, camera depth interval and section half-space through `ViewportNativeOverlayHitResolver`. Legacy identity authority is off every production input: `legacySelectionRectangleHits`, `identityHitResolver`, `requiresLegacyHitFallback`, `legacyViewportHit` and `MeshSourcePresentationLegacyHitFilter` have zero occurrences in Sources and Tests, and neither `Viewport.swift` nor `ViewportInputSurface.swift` names `ViewportIdentityBufferRenderer`, `ViewportIdentityPickRenderPlan` or `ViewportIdentityHitResolver`. Those three and `ViewportPickingReadinessService`/`ViewportPickingReadinessSummary` survive only as a closed five-file cluster with no production entry point, whose sole external mentions are file-path string literals in `CADInteractionQualityAssessmentService`; RK-5 owns deleting it and updating those literals. The single remaining `FIXME(INCOMPLETE_IMPLEMENTATION)` across RupaKit/Sources and Rupa is `Viewport.swift:5397` `makeSpatialOverlaySemanticBuilder`, which RK-IV owns. Each leaf recorded its own reviewer approval and scoped commit. Closing evidence is the fixed snapshot `24396e8e`, where RupaRendering and RupaRenderingTests are clean: `xcodebuild build-for-testing -scheme RupaKit-Package -destination platform=macOS` exit 0, and a serial `RupaRenderingTests` run under an awake display reports 824 tests in 23 suites in 299.348 seconds with the single pre-existing failure `viewportHitTesterSelectsBodyInteriorAndSketchEdges()` at `ViewportSceneTests.swift:3108`. Other modules in that working tree carry a parallel workstream's uncommitted edits, so the verified snapshot is the tree rather than the commit. `depends:RK-3,RK-AXIS` `parallel:none`
- [x] RK-MEASURE-1 The selected-bounds ruler annotation has one placement authority. `RealityViewportSpatialResources` publishes the axes the mounted frame refused to place as a tri-state answer whose absence marks an unprepared camera projection rather than a refusal, the mount relays it beside the grid readout, and `Viewport` reads that published answer instead of recomputing placement from a legacy `ViewportLayout` projection. `ViewportMeasurementBoundsRulerLayout.rulers(for:layout:displayUnit:safeRect:excludedRects:)` is deleted with its last production caller, and a bounds corner that leaves native coordinate precision is reported as the scene failure it is instead of being published as an axis the layout declined `depends:RK-4` `parallel:none`
- [x] RK-5 The legacy rendering and picking backends are gone and production converges on RealityKit-only routes. `ViewportIdentityBufferRenderer`, `ViewportIdentityPickRenderPlan`, `ViewportIdentityPickIndex`, `ViewportIdentityHitResolver`, `ViewportPickingBackend` and the CPU testers they fell back to went in `2f4aff8a`; `ViewportSurfaceRenderer` went in `ae9942e6`, where `RupaResponsivenessBaseline` stopped presenting a retired encoder's timings as the production renderer's; the dead canvas-consumption signposts went in `26c4ea18`; and the unreachable CPU screen-hit picking API went in `80bbb8d1`, which moved its one live segment-versus-rectangle predicate to `ViewportMeasurementRulerCollision` in the `ViewportMeasurement` component. Closing evidence at `80bbb8d1`: `rg` finds zero occurrences across `RupaKit/Sources`, `Rupa` and `RupaKit/Tests` of every deleted symbol and of `MTKView`, `CAMetalLayer`, `MTLRenderPipelineState`, `MTLComputePipelineState`, `makeRenderCommandEncoder`, `makeComputeCommandEncoder`, `CAMetalDrawable`, `currentDrawable`, `MTLCommandQueue` and any legacy `SurfaceError`/`MetalError`/`PipelineError`/`DrawableError` declaration; the one surviving `FIXME(INCOMPLETE_IMPLEMENTATION)` at `Viewport.swift:5405` names RK-IV as its owner and was not orphaned by these removals; `xcodebuild build-for-testing -scheme RupaKit-Package` and `xcodebuild build -scheme Rupa` each report zero `error:` lines with a success marker; and a serial `RupaRenderingTests` run under an awake display reports 745 tests in 22 suites passing in 236.527 seconds, RK-4's single recorded pre-existing failure `viewportHitTesterSelectsBodyInteriorAndSketchEdges()` being absent because RK-5.1 deleted the legacy test that carried it. Other modules in that working tree carry a parallel workstream's uncommitted edits, so the verified snapshot is the tree rather than the commit `depends:RK-4,RK-CLEAN-1` `parallel:none`
- [ ] RK-MEASURE-2 Split the selected-object bounds presentations so the three-dimensional rulers belong to the Measure tool while the text readout accompanies every tool that can draw them. `WorkspaceMeasurementPresentationGate` owns both predicates as one superset relation, `Viewport` takes a separate `showsBoundsReadout` input for the transient status and keeps `showsAutomaticMeasurement` as the native ruler-entity gate, and the Measure context panel presents the same `WorkspaceMeasure.worldBounds` readout the Select panel does so an axis the frame refuses still reports its world-bounds value. Update `ViewportMeasurement/DESIGN.md` first, then prove both predicates with focused RupaUI tests and keep the App round trip readout green `depends:RK-MEASURE-1` `parallel:none`
- [ ] RK-IV 全migration commitを一つの固定snapshotで統合し、timeout付きaffected CPU tests、Apple GPU上のRealityKit render/hit/depth/section/material tests、Release相当App buildとstrict codesign、isolated `.rupa` fixtureのsigned-App操作を一度ずつ行う。Ortho/Persp連続pan/orbit/zoom中にsurface/grid/sketch/handles/measureが同一frameで追従し、4 display modesとStudio/MatCap/Flat/material/random/background/wire/cull、hidden-line/section/selection/hover/rectangle/Mesh element、preview/cancel/failure、Fit/Saved View/UI/API/MCP viewport操作、all existing CAD/Mesh toolsを実経路で確認する。source/generation/selection/Undo/dirty/project bytesとuser project/indexを保護し、旧Metal/Canvas backend不在、frame/publish/cancel/memory ceilingを測定し、元designerが一度conformance reviewしてからfinal coherent commitを作る。既存dirty prerequisiteとtask hunkを分離できず各sprint commitがstandaloneにならない場合は完了にせずexact files/hunksを報告し、task外commitを含むpush、tag、release、deployは行わない `depends:RK-1,RK-2,RK-3,RK-4,RK-5,RK-PERF-1` `parallel:none`
- [ ] RUPA-UI-ALL Every operation the shipped UI presents works from the UI on the RealityKit-only routes. The canvas plane input contract stops fabricating a ray origin, the migration leaves no assertion or audit encoding a deleted backend, and the 10 non-Measure left tools, 7 right utilities, 10 Model-menu operations and 5 Mesh operations are each reported as working from the GUI or as a named limitation with its refusal path `depends:RUPA-TOOLS-AUDIT,RK-IV` `parallel:none`
  - [x] RUPA-UI-ALL.1 `RupaRendering/DESIGN.md` states the canvas plane input contract that `WorkspaceCanvasPlaneInputMapper` owns after the native-camera cutover: a custom construction plane needs a mounted-frame view-ray anchor, the mapper refuses a missing anchor with a typed failure instead of substituting a ground-plane origin, and a plane edge-on to an axis-front preset is a named limitation answered by the view-aligned construction plane request rather than by an oblique projection `depends:none` `parallel:none`
  - [x] RUPA-UI-ALL.2 `WorkspaceCanvasPlaneInputMapper` implements that contract with an `unresolvedViewRayAnchor` failure, `MainView` reports it as a distinct tool status, and the two `EditorSession` canvas tool entry points that no production route calls are marked deprecated with their replacement route `depends:RUPA-UI-ALL.1` `parallel:none`
  - [x] RUPA-UI-ALL.3 The assertions that still encode deleted backends are realigned to the shipped behaviour: the canvas mapper tests supply real view-ray anchors and prove the missing-anchor refusal, the selection quality summary test reads the mounted-frame next result, and the production session-authority audit ignores comment prose while the Outliner drag local stops being named `session` `depends:RUPA-UI-ALL.2` `parallel:none`
  - [x] RUPA-UI-ALL.4 The 33 UI operations are reported as GUI-success verified, lower-layer-only verified, activation/refusal-only verified, broken, or unverified, with each broken route repaired or named as a limitation. Each repair is proven on the snapshot that carries it by running, with a timeout, only the tests its own change can reach; a suite that already ran green is not re-run for a change that cannot reach it `depends:RUPA-UI-ALL.3` `parallel:none`
- [ ] CAD-1 Rupaの下位source・evaluation・package・workspace・UI・semantic Agent/MCP実経路を正本として、画層をScene/Componentの組立階層から分離し、既存`ComponentDefinition`/`ComponentInstance`を使う動的部品、既存`SavedView`/`DrawingProjectionService`を使うモデル/用紙空間と縮尺付き図面ビュー、既存`MeasurementAnnotation`を起点とする異尺度注釈、シートセットのowner・identity・lifetime・上限・失敗・保存schema/version・Undo境界をDESIGN階層へ確定する。具体schema/APIはこのgateのdesigner reviewが、成功/失敗・save/reopen・undo/redoを反証できる検証matrixと既存再利用境界を承認するまで実装しない。AutoCAD全互換、DWG、AutoLISPは対象外とし、設計と進捗だけのscoped commitで閉じる `depends:RK-IV` `parallel:none`
- [ ] CAD-2 Core source ownerがCAD-1で確定した画層分類とview-specific overrideを実装し、scene reparentやComponent配置とは独立したmembership、表示・lock・出力規則を同じProject source authorityへ統合する。RupaUIの画層操作とversioned semantic Agent/MCP操作は同じCore commandを一つのWorkspace transactionで実行し、成功/typed failure、階層独立性、view override、save/reopen、one-entry undo/redoを実経路testで立証してdesigner reviewとscoped commitで閉じる `depends:CAD-1` `parallel:none`
- [ ] CAD-3 Component/CAD evaluation ownerが既存`ComponentDefinition`/`ComponentInstance` identityと配置を保持したまま、CAD-1で確定したtyped definition parameters・instance overrides・式依存をCore evaluationへ接続し、第二の部品graphや文字列推測を作らない。RupaUIの定義/instance編集とversioned semantic Agent/MCP操作は同じCore commandを通り、異なるoverrideの共有definition評価、更新伝播、cycle/missing/type/limit拒否、save/reopen、one-entry undo/redoを実経路testで立証してdesigner reviewとscoped commitで閉じる `depends:CAD-2` `parallel:none`
- [ ] CAD-4 Drawing ownerがmodel sourceを複製せず、CAD-1で確定したpaper space、page、縮尺付きdrawing view、異尺度注釈を既存`SavedView`・evaluated geometry・`DrawingProjectionService`・`MeasurementAnnotation`へ接続し、annotationのpaper-size/scale visibilityとstructured SVG/PDF/PNG出力を同じ意味に保つ。RupaUIのmodel/paper・view・annotation編集とversioned semantic Agent/MCP操作は同じCore commandを通り、複数scale、model更新、hidden/section/layer、unsupported projection、overflow/cancel、save/reopen、one-entry undo/redoを実経路とApple native display testで立証してdesigner reviewとscoped commitで閉じる `depends:CAD-3` `parallel:none`
- [ ] CAD-5 Sheet ownerがCAD-1で確定したordered sheet setと共有metadataをpaper viewsへ安定参照で接続し、missing/cyclic/duplicate referenceやbounded batch超過をtyped failureにして部分出力を公開しない。RupaUIのsheet navigator/reorder/outputとversioned semantic Agent/MCP操作は同じCore command/batchを通り、single/multi-sheet成功、失敗時atomicity、共有metadata反映、save/reopen、one-entry undo/redoを実経路testで立証してdesigner reviewとscoped commitで閉じる `depends:CAD-4` `parallel:none`
- [x] ERR-OBS UI操作の失敗を即座に理解して対処できるようにする。RupaUIの失敗契約を「表示される」から「表示され、かつ記録される」へ変更し、記録が示した実在の失敗を修正する `depends:none` `parallel:none` (d0f4e0f8, 9352d8fc, a12191e7, 8cc4e893, 03adfea7, 4229e65b, c0bc7bac, 69214a4d, 06e325ac, 9967f8bd, 6d098250, 3b5e14b6, ffcb89bb, a370a811, c1575c7b, 1acc387e, b73921ac, 4ae71183, d8e1d4eb, dae294e5, da137df0, 702e003e, fe5b1721, c83773c6, c5d41341)
- [ ] CAD-IV RKとCADの全top-level成果を一つの固定snapshotで統合し、画層、動的部品、model/paper space、縮尺付きview、異尺度注釈、sheet setをRupaUI・versioned Agent API・MCP・package save/reopen・undo/redo・RealityKit native display・structured batch outputの同じproject authority上で操作する。成功、typed failure、stale/cancel/limit、App再起動後の保持、API state適用とnative描画の分離、multi-sheet出力をtimeout付きaffected tests、Apple GPU、signed-App、isolated `.rupa` fixtureで立証し、元designerの一度の総合review後に各scoped commitと最終統合commitを確認する。task外変更・user project/indexを保護し、unsafeなupstreamがあればpushせず、tag/release/deployは行わない `depends:RK-1,RK-2,RK-3,RK-4,RK-5,RK-IV,RUPA-UI-ALL,CAD-1,CAD-2,CAD-3,CAD-4,CAD-5,ERR-OBS` `parallel:none`
- [x] UI-REVIEW-0917-IV Reconciled the correction inventory with production callers, current design and test assertions; source review completed, Apple runtime verification remains blocked by missing Metal Toolchain and is not represented as passing. `depends:UI-REVIEW-0917` `parallel:none`
- [x] UI-P1-0917-IV Apple-platform xcodebuild verified 52 focused case executions: transform/preview/workspace 9, mounted body actions and invalidation 9, input/screen extents 15, producer/world-axis/sketch regression 18, mounted rectangle Escape 1. RupaRenderingTests build-for-testing and RupaUI build passed; no skipped or failing tests in these runs. Full-application and P2 readiness are not claimed. `depends:UI-P1-0917` `parallel:none`
- [x] UI-WIRING-0917-IV Reconciled findings with production callers and existing GUI assertions; bounded macOS xcodebuild executed all 4 WorkspaceTransformMatrixTests successfully (0 skipped). These test TRS conversion and explicit shear refusal, not the complete gizmo-to-inspector GUI workflow; menu coverage executes Box, not all ten CAD operations. `depends:UI-WIRING-0917` `parallel:none`
- [ ] UI-NORMAL-IV Complete integration using non-focus-stealing behavioral checks for production bindings, commands, state publication and Undo/Redo, plus narrowly targeted direct agent inspection of UI presentation and wiring. User stopped foreground XCTest UI automation because it disrupts their screen; do not resume that approach. The resumed Inspector UI test reached the App but failed after normal application termination, not a confirmed crash; no runner remains. Isolated signed App build passed; direct inspection confirmed Box preview/apply, valid evaluation, selection and Inspector property menu, not property change/Undo. Preserve existing verification evidence; whole-UI readiness remains unverified. `depends:UI-NORMAL-1,UI-NORMAL-2,UI-NORMAL-3` `parallel:none`
- [x] UI-REPLACE-IV Verified 224 selected test functions (UI 60, rendering/input 164) through 22 Apple-platform result bundles, with focused reruns closing two fixture failures. Post-commit isolation guard, result-validator self-check, runner syntax and diff checks passed. No foreground automation target remains. Manual visible/OS acceptance remains unverified; no upstream is configured for push. `depends:UI-REPLACE-1` `parallel:none`
- [ ] UI-ACCEPT-IV Reconcile every visible/OS scenario against current-source evidence and affected hidden/GPU regressions; complete only with no unresolved functional failures or unverified required workflows. `depends:UI-ACCEPT-1,UI-ACCEPT-2` `parallel:none`
- [ ] INSPECTOR-LIVE-IV Verify the full Inspector inventory with focused hidden/GPU checks and signed-App immediate updates, errors and Undo; commit scoped fixes before returning to general UI acceptance. `depends:INSPECTOR-LIVE-1,INSPECTOR-LIVE-2` `parallel:none`
- [x] SSOT-IV Integration: 11 UI functions (focused3 and final-ui) and 8 Canvas/native functions (canvas-regression) passed under /tmp/rupa-ssot.IpAwZI; no foreground automation. Incremental Debug App build passed after one 120-second compile timeout; strict deep signature and new Inspector strings verified. Rupa.debug.dylib SHA-256 b6ccefd14a1b1c1539f034fa23c1908f4c976a2c3cab51cf18fff30c2525ddc9. Existing compiler warnings remain; running App not restarted and visible interaction latency not remeasured. No upstream configured. `depends:SSOT-1,INSPECTOR-LATENCY-2` `parallel:none`
- [x] INSPECTOR-INPUT-IV Integrated macOS tests passed: 15 exact test functions / 16 runs, zero failures/skips in /tmp/rupa-inspector-input-integrated.xcresult. Signed Debug App built and strict deep signature verification passed; Rupa.debug.dylib SHA256 b9e7b0b546eddaa982d80abcc9e31345b14d9a942c8e89a3c992c945779d1f2f. Production binding tests cover stale publication/frozen mapping; hidden native control lookup was unavailable and actual screen interaction is not claimed. Running App PID 23965 was not restarted. No upstream configured. `depends:INSPECTOR-INPUT-1` `parallel:none`
- [x] INSPECTOR-FRAME-IV Native mount/query/lifecycle integration: 6 functions passed, no skips (/tmp/rupa-frame-mount-integration-2.xcresult); updated old cancellation-dependent fixture to assert one reusable plan. Signed App build and strict signature verification passed. No foreground UI input or restart; screen input-to-photon latency remains unmeasured. `depends:INSPECTOR-FRAME-1` `parallel:none`
- [x] HANDLE-HANDOFF-IV Native handoff/mount tests passed; App build and strict deep signature verification passed. Normal quit and relaunch completed with binary SHA-256 b252b69875716a18820f9a3d974ab60b345562fcd0e0cb0ab64422f46c7e8ad9. Tests used hidden native input receivers, not foreground events; user-project pixel continuity was not separately recorded. `depends:HANDLE-HANDOFF-1` `parallel:none`
- [x] AFFORDANCE-IV 20 functions/32 runs passed across /tmp/rupa-box-resize-geometry.xcresult, /tmp/rupa-box-handles-native-1.xcresult and /tmp/rupa-box-handles-regression.xcresult. Signed App built, strict signature passed, normally quit/relaunched; Rupa.debug.dylib SHA256 da2e2d80518167a68f2a5cba950de0fc369bea7663b0ca1c8a2875de9436167f. Restart opened blank Untitled; created a temporary 40 mm Box there and visually confirmed face/corner handles. Stopped direct UI actions when user activity was detected; no visible gesture success or pixel-continuity measurement claimed. Source resize capability is positive normal rectangle extrusion only; other shapes retain placement controls. No upstream configured. `depends:AFFORDANCE-1` `parallel:none`
- [x] CENTER-HANDLE-IV Native input and collision regressions passed in /tmp/rupa-inner-marker-green.xcresult and /tmp/rupa-inner-marker-regression.xcresult; signed app build and strict deep signature verification passed. Rupa.debug.dylib SHA256 5b2742e190aedfcad53515d18ff1ce617ee78fa61dada5218a93d200407edac5. Did not restart or send foreground input, preserving the running unsaved document; the running app has not loaded this fix. No upstream configured. `depends:CENTER-HANDLE-1` `parallel:none`
- [x] CORNER-IV Signed App built and normally restarted; latest document saved to .verification/Rupa-restart-backup-20260917-2032.rupa and reopened, source sizes/placement confirmed unchanged. Binary SHA256 d456e88916090d15261f2e517716422daee5d7b03edab0da85a7670d5701a5fb. Native rounded-corner proof passed in /tmp/rupa-corner-native-2-20260917.xcresult; project subdivision/source invariance passed in /tmp/rupa-corner-project-2-20260917.xcresult; Core persistence/Undo/refusal passed in /tmp/rupa-corner-core-4-20260917.xcresult. UI actions stopped on user activity. No push: root has no upstream, swift-CAD includes unrelated unpushed commits. `depends:CORNER-1,CORNER-K1,CORNER-2` `parallel:none`
- [ ] CORNER-3-IV Complete reference-video integration after CORNER-4. Signed build and normal restart succeeded; prior document saved at 21:44:59 to .verification/Rupa-restart-backup-20260917-2032.rupa with Corner 0.011737260954834856 m / Sides 50. Stopped restore when user created another Box; same-document visual comparison remains unverified. Native result /tmp/rupa-corner-interior-native.xcresult passed; binary SHA256 fb84a0b8b0baae54136225c9598efb5f713022c4064733b1e2adf500b357826a. `depends:CORNER-3,CORNER-4` `parallel:none`
- [x] AFFORDANCE-WIDTH-IV Signed app build passed, current two-Box document saved to .verification/Rupa-affordance-backup-20260917-2159.rupa, normally restarted and restored. Selected Box 2 and visually confirmed thicker axes/arcs. `depends:AFFORDANCE-WIDTH-1` `parallel:none`
- [x] SOLID-PREVIEW-IV Native CAD/resize/delayed release passed; diagnostic marker-center Mesh run passed 16 cases. Broader hit-test checks are not all green (Y outer probes and a legacy X repeated-center case); no all-UI claim. Signed Debug build passed; saved/reopened current document and verified Box position/dimensions plus solid display after restart. `depends:SOLID-PREVIEW-1` `parallel:none`
- [x] HANDLE-IV Combined native GPU/crossing/sync/handoff passed (5 runs, /tmp/rupa-handle-integrated.xcresult); face/corner and CAD native input passed. App build exited 0 and strict deep signature passed; dylib SHA256 3d9695b0d3c2775e26dc05ee205d66db6a8e56895bea289c034fe6574250c9ed. Saved latest user document, restarted and reopened both boxes; selected Box dimensions and center matched. Two pre-existing centerScaleY edge-hit runs still fail; no claim of all-UI success. No upstream configured. `depends:HANDLE-CROSS-1,HANDLE-SYNC-1` `parallel:none`
- [x] ALL-HANDLES-IV Combined native tests passed: /tmp/rupa-all-handles-geometry.xcresult (15 runs), /tmp/rupa-all-handles-lifecycle-warm.xcresult (23 runs); WorkspaceBodyResizeTests passed 42 geometry cases and Undo/Redo in /tmp/rupa-all-handles-workspace.xcresult. Signed app built and restarted; dylib SHA256 0cc3370ec2762ed152be61044cdfe9b1ed433f63f1398887b0ad6027a614f686. Saved prior two-box document; user started a new Box after restart, so did not replace that new work by reopening the backup. Two 120s aggregate/compile-plus-test timeouts preceded the split/warm passes. No upstream configured. `depends:ALL-HANDLES-1` `parallel:none`
- [x] CANVAS-STABLE-IV Integrated 8 native/control runs passed in /tmp/rupa-stable-canvas-integrated.xcresult; signed app built, strict signature passed, dylib SHA256 487b0daff6a8edc2b7724cbc449989dd4204338223e917f92066c52eb228f2bf. Saved active document to .verification/Rupa-canvas-stability-backup-20260918-0704.rupa, normally restarted and reopened document F97B84C1; Box rendering confirmed without model edits. Initial label-transfer test exposed lost glyph bounds; persistent host root fixed it. No upstream configured. `depends:CANVAS-STABLE-1,CANVAS-STABLE-2` `parallel:none`
- [x] CANVAS-CHROME-IV Integrated tests: 17 passed; startup/chrome/native follow-up: 3 passed; signed build launched (PID 14747). Pre-restart project saved; reopening withheld after user began new edits. `depends:CANVAS-CHROME-1` `parallel:none`
- [x] AFFORDANCE-CONCEPT-IV Delivered reviewed visual proposal (generated image exec-b65c8970-6953-41ad-8015-b99fa323dbcc.png); corrected planar size handles and arc endpoint depiction. Concept illustration, not implementation or exact handle-placement specification. `depends:AFFORDANCE-CONCEPT-1` `parallel:none`
- [ ] GEOMETRY-EDIT-IV Verify integrated edit entry, native handles, Inspector, live generated geometry, commit/cancel and Undo/Redo without foreground automation; build the app and report measured coverage. `depends:GEOMETRY-FEEDBACK-1,GEOMETRY-EDIT-1,GEOMETRY-EDIT-2,GEOMETRY-EDIT-3` `parallel:none`
- [x] AFFORDANCE-UNIFY-IV Focused native/logic checks: 30 runs passed; final route/center regression: 8 runs passed; all six box faces/eight corners normal/crossing/cancel passed. Broad regression exceeded 120s and was replaced by bounded focused checks, not counted as passing. Final app built, restarted, and saved project 59E3AC78 restored from Rupa-affordance-work-20260918-1040.rupa. No upstream configured. `depends:AFFORDANCE-UNIFY-1` `parallel:none`
- [x] INSPECTOR-COMPACT-IV Final numeric/property tests: 8 passed in /tmp/rupa-inspector-compact-final.xcresult. Hidden host verifies compact dimensions, not colors; actual app confirms colors, rows, precision and model visibility. App built, saved current generation 14, restarted and restored project 59E3AC78; Fit restored model framing. No upstream configured. `depends:INSPECTOR-COMPACT-1` `parallel:none`
